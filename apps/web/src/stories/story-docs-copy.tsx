/** Общий текст UI/* Docs stories (путь как в sidebar RU; `title` в CSF не меняем). */
export function CatalogCrossRefParagraph() {
  return (
    <p className="text-[var(--text-sm)] text-[var(--muted)] max-w-md text-center">
      См. также <strong>Foundations → Каталог компонентов</strong> для согласования в контексте.
    </p>
  );
}

/** JSDoc для story `DesignV2` / sidebar «Витрина». */
export const SHOWCASE_STORY_DESCRIPTION = "Витрина design-v3 (React + CVA, токены)";
