/**
 * Общие настройки построчной виртуализации кокпита планирования
 * (@tanstack/react-virtual): грид Графика и матрица загрузки ресурсов.
 */

/** Запас строк вокруг видимого окна: гасит миганье при быстрой прокрутке
 * и покрывает соседние строки для Tab-навигации по ячейкам. */
export const VIRTUAL_ROW_OVERSCAN = 12;

/**
 * Стартовый прямоугольник скролл-области до реального замера.
 * Без него virtual-core считает окно нулевым (range=null → ни одной строки)
 * в SSR и в happy-dom-тестах, где getBoundingClientRect/offsetHeight дают 0.
 * После mount реальный замер (observe*Rect) перекрывает это значение —
 * виртуализация остаётся честной, просто первый кадр рендерит окно
 * типового вьюпорта, а не пустоту.
 */
export const VIRTUAL_INITIAL_RECT = { width: 1024, height: 768 };
