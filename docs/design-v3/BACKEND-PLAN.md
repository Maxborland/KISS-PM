# Backend-план: разблокировать v3 production frontend

Заземлён в `apps/api` / `packages` / `scripts/seed-dev.ts`. Каждый пункт = endpoint/поле/миграция/сид с точным местом добавления (файл:строка существующего паттерна), изменением модели данных, оценкой и тем, какой v3-экран разблокирует. Фронт уже честно показывает заглушки/empty-state для всего ниже — фейка нет.

Источник: PR #209 (frontend) blockers. Порядок реализации — по value/effort: сперва S/M-эндпоинты comms/admin/auth, затем сиды, затем L (realtime, DM, revert).

---

## communications

### GET meeting detail (composite: participants + notes + action-items + external-links)  `[endpoint · S]`
- **Спека:** GET /api/workspace/meetings/:meetingId -> 200 { meeting: SerializedMeeting, participants: MeetingParticipant[], notes: MeetingNote[], actionItems: MeetingActionItem[], externalLinks: MeetingExternalLink[] }. 400 meeting_id_invalid, 403 (access reason), 404 meeting_not_found, 501 collaboration_not_configured. No request body. Reuse existing read methods + serializers. (Optional later: split per-resource GET /:meetingId/{participants,notes,action-items,external-links}; the composite already returns all four so the panel needs only one call.)
- **Куда:** apps/api/src/collaborationRoutes.ts — add app.get handler right after the PATCH /api/workspace/meetings/:meetingId at line 668 (before POST external-links at 714). Mirror two patterns: (a) resolveMeetingForActor(...) at line 921 for id-parse + access + 404, then (b) the COMPOSITE-detail Promise.all shape of GET /api/workspace/call-rooms/:roomId in apps/api/src/communicationRealtimeRoutes.ts:100-130. Data-source reads already exist: findMeeting (apiTypes.ts:1108), listMeetingParticipants (1123), listMeetingExternalLinks (1131), listMeetingNotes (1136), listMeetingActionItems (1141). Serializers already exist: serializeMeeting (1324), serializeMeetingNote (1342), serializeMeetingActionItem (1351), serializeMeetingExternalLink (1334). Add one tiny serializeMeetingParticipant (createdAt.toISOString()) next to them (MeetingParticipant has no serializer yet). Add an OpenAPI row in apps/api/src/apiDocs/openApiDocument.ts next to the meetings block at lines 191-196.
- **Модель данных:** none — all read methods, tables and serializers already exist; pure route assembly.
- **Разблокирует:** apps/web/src/communications/meetings/meetings-surface.tsx — deletes the SEED_DETAIL hardcoded snapshot + optimistic-merge hack (lines 95-227) and the 'нет GET-ручки деталей' Прототип banner; MeetingDetailPanel renders real server detail. Add client method getMeeting(meetingId) in apps/web/src/communications/lib/comms-client.ts (mirror listMeetings at 583).

### PATCH meeting action-item status (open/done/cancelled)  `[endpoint · M]`
- **Спека:** PATCH /api/workspace/meetings/:meetingId/action-items/:actionItemId -> request { status: 'open'|'done'|'cancelled' } -> 200 { actionItem: SerializedMeetingActionItem }. 400 meeting_action_item_status_invalid, 403 manageDecision.reason, 404 meeting_not_found | meeting_action_item_not_found, 501 collaboration_not_configured. Gate on meeting.value.access.manageDecision.allowed exactly like the POST action-item handler.
- **Куда:** apps/api/src/collaborationRoutes.ts — add app.patch handler right after POST /api/workspace/meetings/:meetingId/action-items at line 814. Mirror that POST for resolveMeetingForActor + manage-gate + denied-audit + runDataSourceTransaction + serializeMeetingActionItem (1351), and mirror PATCH meeting (line 668) for the partial-update + audit shape (actionType 'collaboration.meeting_action_item_updated'). Add domain parser parseMeetingActionItemStatus in packages/domain/src/collaboration.ts next to meetingActionItemStatuses (line 74) — mirror existing parseMeetingStatus (imported at collaborationRoutes.ts:15). Add data-source method updateMeetingActionItem (+ optional findMeetingActionItem) to apps/api/src/apiTypes.ts next to createMeetingActionItem (1137) / listMeetingActionItems (1141) — copy the shape of updateKnowledgeActionItem (apiTypes.ts:1280). Implement in packages/persistence/src/collaborationRepository.ts next to createMeetingActionItem (1239) / listMeetingActionItems (1250), mirroring knowledgeRepository.ts updateKnowledgeActionItem (327-348). Bind it in packages/persistence/src/collaborationRepositoryAdapters.ts next to the createMeetingActionItem bind (167). Add OpenAPI row near openApiDocument.ts:196.
- **Модель данных:** NO migration — meeting_action_items.status column already exists and is written by createMeetingActionItem and read by mapMeetingActionItem. Only a new repository UPDATE method + adapter binding + a domain status parser.
- **Разблокирует:** apps/web/src/communications/meetings/meetings-surface.tsx ActionItemsCard (lines 494-555): turns the inert checkbox + 'status ВСЕГДА open' note into a real toggle; add client method patchActionItem(meetingId, actionItemId, {status}) in comms-client.ts and an updateActionItem in useMeetings (use-comms.ts:348).

### SSE realtime stream for new messages + notifications (replace poll/refetch)  `[endpoint · L]`
- **Спека:** GET /api/workspace/realtime/events (text/event-stream, cookie-auth). Events: { type:'message.created', conversationId, entityType, entityId, messageId } and { type:'notification.created', notificationId, notificationType } plus 15s heartbeat. Scope delivery to the actor (notifications) and to entities the actor can read (messages). 401 session_required. Pure server-push; client keeps existing GET for backfill.
- **Куда:** New file apps/api/src/collaborationEventBus.ts — copy apps/api/src/planningEventBus.ts (InMemory pub/sub keyed by tenantId+userId for notifications and tenantId+entity for messages; export subscribe/emit). New file apps/api/src/collaborationEventsRoute.ts — copy apps/api/src/planningEventsRoute.ts verbatim (streamSSE from 'hono/streaming', heartbeat, stream.sleep, unsubscribe). Register it in apps/api/src/app.ts beside registerCollaborationRoutes at line 266. Emit events inside apps/api/src/collaborationRoutes.ts: after createDiscussionMessage (the transaction at line 164) and at every createUserNotification call (e.g. line 195 mention, plus meeting_invite/meeting_action_item in the meeting handlers). Pattern precedent: planning already does notifyPlanVersionChanged → SSE.
- **Модель данных:** none — in-memory event bus (mirrors InMemoryPlanningEventPublisher); Redis fan-out is an optional later swap like planningRedisEventBus.ts. No table, no seed.
- **Разблокирует:** apps/web/src/communications/chat/chat-surface.tsx (live message append instead of reloadMessages polling) and apps/web/src/communications/notifications/notifications-surface.tsx (live feed). Add client.subscribeRealtime() (EventSource) in comms-client.ts; wire into use-comms.ts useConversation/useNotifications to drop manual reloads.

### Numeric unread aggregate (chrome/comms badge) without enumerating entities  `[endpoint · M]`
- **Спека:** GET /api/workspace/unread-summary -> 200 { notifications: number, conversations: number }. notifications = count of unread UserNotifications for actor; conversations = sum of ConversationReadState.unreadCount across the actor's readable conversations. 401 session_required, 501 collaboration_not_configured. Single cheap call for the nav badge.
- **Куда:** apps/api/src/collaborationRoutes.ts — add app.get('/api/workspace/unread-summary') near GET /api/workspace/notifications at line 465. For notifications reuse dataSource.listUserNotifications (apiTypes.ts:1082) with status 'unread' (or add a thin countUserNotifications method beside it for efficiency). For conversations reuse getConversationReadState (apiTypes.ts:1068) already used by the conversations list at collaborationRoutes.ts:91-97 (or add sumConversationUnread aggregate). Mirror the actor-resolution + 501 guard used throughout this file.
- **Модель данных:** no migration; conversation_read_state.unread_count and user_notifications already exist. Optional: add count/sum data-source methods (+ persistence COUNT queries) for O(1) instead of fetch-and-length.
- **Разблокирует:** apps/web/src/communications/ui/comms-frame.tsx tab badges and apps/web/src/views/layout/workspace-chrome.tsx top-nav unread dot — currently no numeric source; add client.getUnreadSummary() in comms-client.ts.

### Direct-message (1:1) conversation kind with explicit membership  `[migration · L]`
- **Спека:** POST /api/workspace/conversations/direct { recipientUserId } -> 201 { conversation } (idempotent: returns existing DM thread for the pair). GET /api/workspace/conversations/direct -> { conversations: (Conversation & { readState, otherUserId })[] }. Reuses existing message routes (listMessages/postMessage/markRead at collaborationRoutes.ts:106/139/451) by conversationId. Access = membership in the DM, not entity-RBAC.
- **Куда:** Domain: extend conversationTypes (packages/domain/src/collaboration.ts:17) with 'direct'. Add a conversation membership concept — new table conversation_participants(tenantId, conversationId, userId) in packages/persistence (drizzle schema + migration) because conversations today carry no member list (access derives from entityType/entityId). Add resolver branch in apps/api/src/collaboration/entityAccess.ts so a DM conversation authorizes by membership instead of resolveCollaborationEntityAccess. New routes + ensureDirectConversation/listDirectConversations/upsertConversationParticipant data-source methods in apps/api/src/apiTypes.ts (mirror ensureConversation/listConversationsByEntity at 993). Add routes in collaborationRoutes.ts beside GET /api/workspace/conversations (54). resolveConversationForActor at line 894 must accept DM membership.
- **Модель данных:** MIGRATION: new conversation_participants table + 'direct' conversationType; entityType for a DM thread is synthetic (e.g. entityType still required by Conversation — use a dedicated sentinel or relax). Seed: add 1-2 DM threads between u-anna/u-ivan in scripts/seed-dev.ts + packages/test-fixtures so the chat DM tab has data.
- **Разблокирует:** apps/web/src/communications/chat/chat-surface.tsx DM/people pane (today chat is purely entity-scoped channels; no 1:1). Needs comms-client listDirectConversations/createDirect + a DM tab in chat-surface.

### User presence (online / last-seen) for chat + channels  `[endpoint · M]`
- **Спека:** POST /api/workspace/presence/heartbeat -> 200 { ok:true } (call every ~30s while a comms surface is mounted). GET /api/workspace/presence?userIds=a,b,c -> { presence: { userId, status:'online'|'away'|'offline', lastSeenAt }[] }. online = heartbeat within 60s, away within 5m, else offline. 401 session_required.
- **Куда:** New presence registry apps/api/src/communicationsPresence.ts — in-memory Map<tenantId, Map<userId, lastSeenMs>> updated on heartbeat (same in-process-singleton style as planningEventBus.ts; optionally also publish presence.changed onto the new collaborationEventBus from item 3). New routes in apps/api/src/collaborationRoutes.ts near the conversations block (line 54) or a small new presenceRoutes.ts registered in app.ts beside line 266. Mirror the actor-resolution guard used everywhere in this file.
- **Модель данных:** none for MVP — ephemeral in-memory store (lost on restart, acceptable for presence). Optional later: user_presence(tenantId,userId,lastSeenAt,status) table + migration for multi-instance. No seed.
- **Разблокирует:** apps/web/src/communications/chat/chat-surface.tsx presence dots and apps/web/src/communications/channels/channels-surface.tsx member online indicators (no presence source today). Add client.heartbeat()/getPresence() in comms-client.ts; ping on an interval in comms-runtime.

### Current-actor projection so comms stops hardcoding ME (mostly already exists)  `[endpoint · S]`
- **Спека:** ALREADY EXISTS: GET /api/auth/me (apps/api/src/authRoutes.ts:119) returns { user, permissions, workspace } with the authenticated user id+name — this fully satisfies 'current actor'. Backend ADD is optional: a thin comms-scoped alias GET /api/workspace/me -> { user: { id, name } } if you don't want comms to depend on the auth payload. The real fix is frontend: replace const ME = 'u-anna'.
- **Куда:** If adding the alias: apps/api/src/workspaceUserRoutes.ts — add app.get('/api/workspace/me') beside GET /api/workspace/users at line 26 (same getSessionActorFromHeaders + listWorkspaceUsers().find(id===actor.id) lookup already done in /api/auth/me at authRoutes.ts:128-135). Otherwise reuse /api/auth/me directly. Add client.getMe() in apps/web/src/communications/lib/comms-client.ts (mirror listUsers at 640).
- **Модель данных:** none — listWorkspaceUsers + session actor already provide id/name; no table, no migration, no seed.
- **Разблокирует:** apps/web/src/communications/chat/chat-surface.tsx line 29 (const ME = 'u-anna') and its uses at 205/297/403 (own-message highlight, own-reaction toggle); also lets meetings-surface stop stamping authorUserId:'u-anna' on optimistic notes. Expose currentUserId via comms-runtime so all comms surfaces read it.


---

## admin

### GET assignable permission catalog endpoint  `[endpoint · S]`
- **Спека:** GET /api/workspace/access-roles/permission-catalog. No body. Auth: session cookie required (401 session_required). Gate with canReadAccessProfiles (tenant.access_profiles.read) -> 403 {error: reason}. Response 200: {"permissions": string[]} = the 60-key access-control catalog, returned verbatim from the exported const `permissions` (packages/access-control/src/index.ts:3-67). No persistence -> no `persistence_not_configured` guard. OPTIONAL convenience field for the grouped UI: also return {"groups":[{"key":"tenant","permissions":[...]}, ...]} grouped by first dot-segment, but the flat list alone fully satisfies the client (it already groups itself).
- **Куда:** apps/api/src/accessRoleRoutes.ts — add a new `app.get("/api/workspace/access-roles/permission-catalog", ...)` handler INSIDE registerAccessRoleRoutes, placed before the PATCH/DELETE `/:roleId` routes. Mirror the existing list handler `app.get("/api/workspace/access-roles", ...)` at apps/api/src/accessRoleRoutes.ts:144-170 (same actor/decision/appendAccessRoleDeniedAudit shape, minus the dataSource call). Import `permissions` from "@kiss-pm/access-control" (file already imports canReadAccessProfiles from that package at lines 1-5). No registration change needed (registerAccessRoleRoutes already wired at apps/api/src/app.ts:262).
- **Модель данных:** none — reuse the existing exported const `permissions` (packages/access-control/src/index.ts:3) and `isPermission` validator (index.ts:93). No new table, no domain-type change.
- **Разблокирует:** Roles admin surface permission picker: apps/web/src/admin/roles/roles-surface.tsx PermissionChecklist + CreateRole/EditRole dialogs. Replaces the hand-duplicated client-side enum apps/web/src/admin/lib/permissions-catalog.ts (ALL_PERMISSIONS, explicitly flagged as a static mirror because no REST endpoint exists). Add a `listPermissionCatalog()` method to apps/web/src/admin/lib/admin-client.ts (mirror listAccessRoles at admin-client.ts:97) so the catalog is fetched, not compiled in.

### tenant_security_policies table + schema + repo + dataSource port  `[migration · M]`
- **Спека:** New one-row-per-tenant table `tenant_security_policies`: tenant_id text PK references tenants(id) on delete cascade; two_factor_required boolean not null default false; session_timeout_hours integer not null default 24; sso_saml_enabled boolean not null default false; domain_allowlist jsonb $type<string[]> not null default '[]'::jsonb; created_at + updated_at timestamptz not null. PK = tenant_id (singleton per tenant). New domain/record type SecurityPolicyRecord = { tenantId; twoFactorRequired: boolean; sessionTimeoutHours: number; ssoSamlEnabled: boolean; domainAllowlist: string[]; updatedAt: string }.
- **Куда:** (1) New migration file packages/persistence/migrations/0044_phase_admin_tenant_security_policy.sql (next free number; 0043 is current max). Mirror the access_profiles DDL conventions (jsonb permissions + tenant FK) and the workspace_config migration packages/persistence/migrations/0002_phase_2_3_workspace_config.sql. (2) Add `tenantSecurityPolicies` pgTable to packages/persistence/src/schema.ts, mirroring `accessProfiles` at schema.ts:24-42 (jsonb().$type<string[]>() for domain_allowlist as at schema.ts:32). (3) Add getSecurityPolicy(tenantId) + upsertSecurityPolicy(input) + a mapSecurityPolicyRecord mapper to packages/persistence/src/repositories.ts — mirror updateAccessProfile at repositories.ts:354-374, but implement upsert via insert(...).onConflictDoUpdate on tenant_id. (4) Add optional methods `getSecurityPolicy?(tenantId): Promise<SecurityPolicyRecord|undefined>` and `upsertSecurityPolicy?(input: SecurityPolicyRecord): Promise<SecurityPolicyRecord>` plus the SecurityPolicyRecord type to ApiTenantDataSource in apps/api/src/apiTypes.ts, after the access-profile methods (~apiTypes.ts:427), mirroring AccessProfileRecord at apiTypes.ts:109.
- **Модель данных:** NEW table tenant_security_policies (DDL above) + NEW SecurityPolicyRecord type + NEW drizzle table `tenantSecurityPolicies` + 2 new dataSource port methods. This is the storage layer the security-policy endpoint depends on.
- **Разблокирует:** Storage backing for the «Политики безопасности» (2FA/SSO/whitelist) card; without it the endpoint item below has nowhere to read/write.

### GET+PUT tenant security policy endpoint  `[endpoint · M]`
- **Спека:** GET /api/workspace/config/security-policy. Auth required (401 session_required). Gate canReadWorkspaceConfig (tenant.workspace_config.read) -> 403. If dataSource.getSecurityPolicy missing -> 501 persistence_not_configured. Response 200: {"securityPolicy": {"twoFactorRequired": boolean, "sessionTimeoutHours": number, "ssoSamlEnabled": boolean, "domainAllowlist": string[], "updatedAt": string|null}}. When no row exists return defaults {twoFactorRequired:false, sessionTimeoutHours:24, ssoSamlEnabled:false, domainAllowlist:[], updatedAt:null}. PUT /api/workspace/config/security-policy (full replace). Gate canManageWorkspaceConfig (tenant.workspace_config.manage) -> 403 (+ denied audit). Request body: {twoFactorRequired:boolean, sessionTimeoutHours:number, ssoSamlEnabled:boolean, domainAllowlist:string[]}. Validation -> 400 invalid_security_policy: sessionTimeoutHours integer 1..168; both booleans real booleans; domainAllowlist array of trimmed lowercase domains (max ~50, each matching a host regex). On success upsertSecurityPolicy + appendManagementAuditEvent (actionType "workspace.security_policy.updated", sourceWorkflow "single_workspace_config", beforeState=prev, afterState=updated, permissionResult=decision). Response 200: {"securityPolicy": {...}}. (Spec keeps PUT per the card contract; repo convention is POST/PATCH but Hono app.put works identically.)
- **Куда:** apps/api/src/workspaceConfigRoutes.ts — add the two handlers inside registerWorkspaceConfigRoutes. Mirror GET `app.get("/api/workspace/config/custom-fields", ...)` at workspaceConfigRoutes.ts:71-100 for the read path (canReadWorkspaceConfig already imported at workspaceConfigRoutes.ts:3), and mirror POST `app.post("/api/workspace/config/custom-fields", ...)` at workspaceConfigRoutes.ts:101 for the write path (canManageWorkspaceConfig + runDataSourceTransaction + appendManagementAuditEvent, sourceWorkflow "single_workspace_config"). Add a parseSecurityPolicyBody validator alongside the existing config parsers. No new registration — registerWorkspaceConfigRoutes is already wired at apps/api/src/app.ts:286. NOTE: do NOT use controlSurfaceRoutes.ts — that file is the dashboard control-surface builder (validateControlSurfaceDefinition / requiredPermissions), an unrelated schema as the prompt noted.
- **Модель данных:** Depends on the migration item (tenant_security_policies table + getSecurityPolicy/upsertSecurityPolicy port methods + SecurityPolicyRecord). No additional model change in this item.
- **Разблокирует:** The «Политики безопасности» card in the v3 admin surface — currently explicitly out-of-contract (apps/web/src/admin/users/users-surface.tsx:50 banner: «Политики безопасности (2FA/SSO/whitelist) вне контракта — не показаны»). Add getSecurityPolicy()/updateSecurityPolicy() to apps/web/src/admin/lib/admin-client.ts (mirror updateAccessRole at admin-client.ts:99) to render and persist the card.

### Seed default security policy for demo tenants  `[seed · S]`
- **Спека:** Seed one tenant_security_policies row per demo tenant (at least tenant-alpha) so GET returns real data on a fresh dev DB and DB-backed tests have a baseline. Row shape: {tenantId:"tenant-alpha", twoFactorRequired:false, sessionTimeoutHours:24, ssoSamlEnabled:false, domainAllowlist:["kiss-pm.dev"], createdAt:now, updatedAt:now}. Inserted as part of the existing tenant dataset seed pass.
- **Куда:** (1) Extend SeedTenantDataset + seedTenantDataset in packages/persistence/src/seed.ts to accept and insert a `securityPolicies` array (mirror how accessProfiles is consumed in seed.ts / repositories.ts:336-352). (2) Add a `securityPolicies: [...]` key to the `dataset` object in scripts/seed-dev.ts:32-43, next to the existing `accessProfiles`/`positions` keys. (3) Optionally surface a default in packages/test-fixtures createDemoTenantDataset (imported at scripts/seed-dev.ts:1) so unit/DB fixtures get the row automatically.
- **Модель данных:** Depends on the migration item table tenant_security_policies. No new types beyond SecurityPolicyRecord already defined there.
- **Разблокирует:** Makes the «Политики безопасности» card show seeded values on first load in dev and keeps the security-policy endpoint integration/DB tests deterministic.


---

## auth

### GET /api/auth/sessions — enumerate current user's active sessions  `[endpoint · M]`
- **Спека:** GET /api/auth/sessions. Auth: session cookie only (no body). 401 {error:"session_required"} when no/expired session. 200 {sessions: Array<{id:string, createdAt:string(ISO), expiresAt:string(ISO), current:boolean}>}. Rows = user_sessions WHERE tenant_id=actor.tenantId AND user_id=actor.id AND expires_at>now, ordered created_at DESC. current = (row.tokenHash === hashSessionToken(cookieToken)) so the calling device is flagged. No request params. Minimal field set (matches existing columns) — device label/IP come from item 3.
- **Куда:** Route: apps/api/src/authRoutes.ts — add `app.get("/api/auth/sessions", ...)` after the /api/auth/me handler (authRoutes.ts:119-141); mirror that handler exactly for actor resolution via getSessionActorFromHeaders + the cookie read it already does (authRoutes.ts:120-126). Read the raw cookie token with getSessionTokenFromCookie (already imported, used at authRoutes.ts:106) + hashSessionToken (imported authRoutes.ts:1) to compute `current`. New optional dataSource method `listSessionsByUserId(tenantId, userId): Promise<UserSessionRecord[]>` in apps/api/src/apiTypes.ts after deleteSessionsByUserId (apiTypes.ts:765); add the same to RepositoryDataSource interface in packages/persistence/src/repositories.ts:196 and implement it after deleteSessionsByUserId (repositories.ts:613-617) by mirroring its select/where (and(eq tenant_id, eq user_id)). In-memory dataSource has no session methods (none in inMemoryTenantDataSource.ts), so guard the route with `if (!dataSource.listSessionsByUserId) return context.json({error:"auth_not_configured"},501)` exactly like login (authRoutes.ts:34-36).
- **Модель данных:** none — reuses existing user_sessions columns (id, tenant_id, user_id, token_hash, expires_at, created_at; schema.ts:694-713). UserSessionRecord (repositories.ts:118-124) already carries all needed fields except createdAt — add `createdAt: Date` to the SELECT projection (currently findSessionByTokenHash drops it, repositories.ts:600-607) or return it directly from the new list method.
- **Разблокирует:** avatar-menu «Активные сессии» — replaces the honest placeholder "Список активных устройств появится после контракта сессий" (apps/web/src/auth/avatar-menu/avatar-menu-surface.tsx:226-228) with a real session list; needs a new auth-client method (apps/web/src/auth/lib/auth-client.ts, alongside me() at line 133).

### DELETE /api/auth/sessions/:id — revoke a specific session (remote logout)  `[endpoint · M]`
- **Спека:** DELETE /api/auth/sessions/:id. Auth: session cookie. Deletes exactly one row scoped to (tenant_id=actor.tenantId, user_id=actor.id, id=:id) so a user can only revoke their OWN sessions. 200 {status:"ok"} on delete; 404 {error:"session_not_found"} when no matching owned row; 401 {error:"session_required"} without a session. Idempotent-ish: deleting the current session is allowed (acts like logout for that device). No request body.
- **Куда:** Route: apps/api/src/authRoutes.ts — add `app.delete("/api/auth/sessions/:id", ...)` directly after the GET sessions handler from item 1; mirror logout for the delete+cookie pattern (authRoutes.ts:104-117) and /api/auth/me for actor resolution (authRoutes.ts:119-126). Read `context.req.param("id")`. New optional dataSource method `deleteSessionById(tenantId, userId, id): Promise<number>` (rows affected, for the 404) in apps/api/src/apiTypes.ts after listSessionsByUserId; add to RepositoryDataSource (packages/persistence/src/repositories.ts:196) and implement after deleteSessionsByUserId (repositories.ts:613-617) mirroring its `.delete(userSessions).where(and(eq tenant_id, eq user_id, eq id))` and returning rowCount. Note: this is a state-mutating route — confirm whether it needs the x-kiss-pm-action same-origin header (see requiresSameOriginActionHeader exclusions referenced in authRegistrationRoutes.ts:36-38); logout is currently exempt, so mirror logout's exemption decision.
- **Модель данных:** none.
- **Разблокирует:** per-row «Завершить сессию»/«Выйти на этом устройстве» control in avatar-menu «Активные сессии» (apps/web/src/auth/avatar-menu/avatar-menu-surface.tsx ~line 226-231, next to the existing global «Выйти из системы» button).

### user_sessions device metadata (user_agent, ip, last_seen_at) for real device rows  `[migration · L]`
- **Спека:** ALTER TABLE user_sessions ADD COLUMN user_agent text, ADD COLUMN ip text, ADD COLUMN last_seen_at timestamptz (all nullable). Capture user_agent (from request `user-agent` header) + ip (getClientIp, already used in auth flows) at session creation; bump last_seen_at on each successful session validation. Extend GET /api/auth/sessions response (item 1) with: deviceLabel:string|null (derived from user_agent), ip:string|null, lastSeenAt:string(ISO)|null. Derivation of a friendly label can be a small UA-parse helper in apps/api/src.
- **Куда:** Schema: packages/persistence/src/schema.ts:694-713 (add the three columns to userSessions pgTable). New SQL migration under packages/persistence/migrations + matching meta snapshot (mirror the existing 0001_gorgeous_moon_knight.sql / meta/0001_snapshot.json pair). Domain type: add userAgent/ip/lastSeenAt to UserSessionRecord (packages/persistence/src/repositories.ts:118-124) and to createSession insert (repositories.ts:587-592). Capture at write sites: login (apps/api/src/authRoutes.ts:77-83) and register auto-login (apps/api/src/authRegistrationRoutes.ts:149-155) — pass user_agent via context.req.header("user-agent") and ip via getClientIp (authRoutes.ts:45-48 pattern). last_seen_at bump belongs in the session-validation path (getSessionActorFromHeaders / findSessionByTokenHash, repositories.ts:593-608).
- **Модель данных:** Migration required: 3 new nullable columns on user_sessions; column list in schema.ts tableColumns.user_sessions (schema.ts:5094-5101) must also be extended to keep the persistence column registry in sync.
- **Разблокирует:** real device names + last-active timestamps in «Активные сессии» (replaces the dashed placeholder in avatar-menu-surface.tsx:226-228 with truthful rows); without it the list shows only opaque session ids + dates.

### Dev-mode reset-token surface on password-reset/request (parity with mock)  `[field · S]`
- **Спека:** Augment POST /api/auth/password-reset/request response. Prod stays exactly {status:"ok"} at 202 (anti-enumeration; authRegistrationRoutes.ts:269). When a dev flag is enabled AND the email matched a credential, ALSO return `devToken: rawToken`: response 202 {status:"ok", devToken?:string}. This mirrors the frontend mock byte-for-byte (apps/web/src/auth/lib/mock-auth-backend.ts:373 returns `{status:"ok", devToken: rawToken}`). devToken is NEVER set when the flag is off or the email is unknown. NOTE confirmed during audit: EmailProvider wiring is in-memory NO-OP only (app.ts:85 `options.emailProvider ?? createInMemoryEmailProvider()`, emailProvider.ts:21-29) — no SMTP/SES exists, so on a real backend the reset token is currently undeliverable; this dev surface is what makes the reset-confirm flow testable.
- **Куда:** apps/api/src/authRegistrationRoutes.ts — at the success return (authRegistrationRoutes.ts:269) include devToken when a new dep flag is true and `credential` existed (rawToken is in scope at line 244). Add the flag to ApiRouteDeps and thread it through app.ts mirroring `enableDevTenantRoutes` (app.ts:88 `options.enableDevTenantRoutes ?? false`) — same env/option gating style. Alternative (equivalent) surface: GET /api/auth/password-reset/dev-last-token reading the InMemoryEmailProvider.lastPasswordReset (emailProvider.ts:16-19) — but the in-response devToken is preferred since it matches the mock contract the frontend already speaks.
- **Модель данных:** none.
- **Разблокирует:** Storybook/dev reset-confirm flow on the REAL backend without a mailbox — reset-confirm-surface.tsx + apps/web/src/auth/password-reset/password-reset.stories.tsx (frontend already consumes devToken in the mock per mock-auth-backend.ts:24,362-373, so wiring it real removes the prod-vs-mock divergence).

### Real EmailProvider (SMTP/SES) implementation + env wiring  `[job · M]`
- **Спека:** Implement a production EmailProvider that actually delivers sendPasswordReset emails (subject/body with resetUrl). Same EmailProvider signature as today (emailProvider.ts:12-14). Add a factory `createEmailProviderFromEnv()` selecting SMTP/SES vs in-memory by env (mirror createStorageProviderFromEnv / createVideoProviderFromEnv used at app.ts:83-84), so app.ts:85 becomes `options.emailProvider ?? createEmailProviderFromEnv()`. No HTTP route change — this fixes the silent no-op delivery confirmed in the audit (only createInMemoryEmailProvider exists today).
- **Куда:** New file apps/api/src/emailProvider.ts sibling (e.g. createSmtpEmailProvider) implementing the existing EmailProvider type (emailProvider.ts:12-14); add createEmailProviderFromEnv mirroring the *FromEnv pattern already imported in app.ts (createStorageProviderFromEnv/createVideoProviderFromEnv, app.ts:83-84). Swap the default at app.ts:85. The call site sendPasswordReset is already wired (authRegistrationRoutes.ts:255-259) — no route edits.
- **Модель данных:** none.
- **Разблокирует:** production password-reset/request flow (apps/web/src/auth/password-reset/reset-request-surface.tsx) — today the token is captured in-memory and never sent, so a real (non-dev) user can never complete reset; this is the prod blocker behind item 4's dev workaround.


---

## planning-commits-revert

### Capture inverse (compensating) commands into the planning audit event at apply time  `[field · M]`
- **Спека:** FOUNDATION for revert (required by both options). Today GET /api/tenant/current/audit-events carries beforeState as COUNTS only (apps/api/src/planning/planningRouteHelpers.ts:139 summarizeSnapshot → {projectId,planVersion,taskCount,assignmentCount,dependencyCount}), so the client cannot reconstruct old field values and revert is dead. The data model stores no per-version historical snapshot, so the inverse MUST be computed when the before-snapshot is still in hand (inside the apply transaction). Change: in apply-command (apps/api/src/planning/registerPlanningRoutes.ts:299-315) and apply-command-batch (registerPlanningRoutes.ts:481-500), BEFORE applying, build the before read-model and compute reversible inverse commands, then add them to audit afterState. New shape:
  afterState: { planVersion, changedTaskIds, changedAssignmentIds, changedDependencyIds, revertCommands: PlanningCommand[] }
Computation (server, identical logic to client):
  const beforeRm = createPlanningReadModel(snapshot, { includeResourceExceptions: true });
  const revertCommands = [/* batch: parsed.value.commands; single: [parsed.value.command] */].slice().reverse().flatMap(cmd => buildCompensatingCommands(cmd, beforeRm));
revertCommands is empty for irreversible ops (task.create / move_wbs / assignment.upsert) — that is fine and signals revertible=false. `snapshot` is the pre-change snapshot already fetched at registerPlanningRoutes.ts:251 (single) / :446 (batch); createPlanningReadModel is already imported there.
- **Куда:** Port the inverse logic to a server module apps/api/src/planning/planningCompensation.ts (1:1 mirror of packages/planning-client/src/undo/buildCompensatingCommands.ts:22 — it reads only readModel.authored.tasks/.dependencies which equal snapshot.tasks/.dependencies per apps/api/src/planning/planningReadModel.ts:49-50, so it runs unchanged server-side). Cleaner long-term: lift that one function into @kiss-pm/domain and have BOTH api and planning-client import it (single source of truth) — minimal version just ports it. Wire the two call sites in registerPlanningRoutes.ts:299 and :481, mirroring the existing buildAuditInput object literal at registerPlanningRoutes.ts:481-500.
- **Модель данных:** none — afterState is a JSON column typed Record<string,unknown>|null (apiTypes.ts ManagementAuditEventInput.afterState:402, dataSource.appendAuditEvent input.afterState:939, AuditEventListItem.afterState:124). Adding a nested revertCommands key needs no migration. It also flows to the client automatically: auditRoutes.ts:50-53 spreads ...event so afterState passes through untouched.
- **Разблокирует:** v3 Коммиты surface (apps/web/src/app/projects/[id]/commits/page.tsx). Supplies the inverse the client needs; the live branch of loadCommits (apps/web/src/delivery/lib/use-planning.ts:205-229) currently hardcodes latestRevert=null and revertible=false specifically because beforeState was counts-only.

### POST .../planning/revert/:auditEventId — server-authoritative revert endpoint (RECOMMENDED)  `[endpoint · M]`
- **Спека:** RECOMMENDED option (a): cleaner than client-side rebuild because revert is itself a governed mutation — permission-checked, plan-version-guarded, transactional, and audited — with no read-model or inverse logic leaked to the client.
Method+path: POST /api/workspace/projects/:projectId/planning/revert/:auditEventId
Request: { clientPlanVersion: number, idempotencyKey?: string }
Response 200: { revertedAuditEventId: string, newPlanVersion: number, auditEventId: string, applied: { changedTaskIds: string[], changedAssignmentIds: string[], changedDependencyIds: string[] }, readModel: PlanningReadModel }
Errors (mirror existing handlers): 400 planning_command_invalid/invalid params; 401 session_required; 403 <permission reason> (run permissionForCommand per revert command, registerPlanningRoutes.ts:380-396); 404 project_not_found | audit_event_not_found; 409 plan_version_conflict | planning_precondition_failed | audit_event_not_revertible (empty revertCommands).
Handler flow: (1) load the audit event server-side via deps.dataSource.listAuditEventsByTenantId(actor.tenantId, { projectId, limit: 100 }) (same accessor auditRoutes.ts:44) and find id===auditEventId — avoids a new persistence method; (2) validate sourceWorkflow==='planning' and read event.afterState.revertCommands (from item 1); empty/missing → 409 audit_event_not_revertible; (3) run the SAME governed transaction body as apply-command-batch over those revertCommands, writing a fresh audit event with actionType 'planning.command.reverted' and commandInput { revertedAuditEventId: auditEventId, commands: revertCommands }. The clientPlanVersion guard (registerPlanningRoutes.ts:448) inherently enforces the mock's 'latest revertible commit only' rule.
- **Куда:** apps/api/src/planning/registerPlanningRoutes.ts — add the new app.post handler immediately after the apply-command-batch block (after registerPlanningRoutes.ts:539), MIRRORING the apply-command-batch handler at registerPlanningRoutes.ts:363-539 (reuse runDataSourceTransaction, lockTenantResourcePlanning, previewPlanningCommands, isBlockingValidationIssue gate, incrementPlanVersion, appendPlanningAuditIfConfigured, persistPlanningNotifications, emitPlanVersionFromBody). Add a parseAuditEventIdParam to apps/api/src/routeParamParsers.ts mirroring parseProjectIdParam, and a parseAuditEventRouteParam helper in apps/api/src/planning/planningRouteHelpers.ts mirroring parseProjectRouteParam:48. Optionally register the path in apps/api/src/apiDocs/openApiDocument.ts next to the existing apply-command-batch entry.
- **Модель данных:** none required. Reuses listAuditEventsByTenantId (apiTypes.ts:945) to fetch the event, so no new persistence method and no migration. Optional ergonomic add (not needed for v1): findAuditEventById(tenantId, projectId, auditEventId) on ApiTenantDataSource near apiTypes.ts:945 + its repo impl, to replace the limit:100 scan.
- **Разблокирует:** The live 'Откатить' control in apps/web/src/delivery/commits/commits-surface.tsx (onRevert:64, inline link:107, button:142), currently dead per the note in apps/web/src/app/projects/[id]/commits/page.tsx:5. Frontend wiring (out of scope, noted for handoff): use-planning.ts loadCommits live branch (:216-228) sets revertible = (event.afterState.revertCommands?.length>0) and latestRevert from the newest such planning event; onRevert calls the new endpoint instead of buildCompensatingCommands+applyBatch. ALTERNATIVE option (b), lower effort, NOT recommended: skip this endpoint entirely — with item 1 done, afterState.revertCommands is already returned by GET /api/tenant/current/audit-events (auditRoutes.ts:50), so the client can populate latestRevert.commands directly and replay them through the EXISTING POST apply-command-batch (use-planning.ts applyBatch:141). Saves the new route+parser but is client-trusted (client echoes server-computed commands; still re-validated by the governed batch path).


---

## seeds

### Расширить createDemoTenantDataset: +4 демо-пользователя ролей для tenant-alpha  `[seed · S]`
- **Спека:** В packages/test-fixtures/src/index.ts добавить в массив users 4 объекта через createTenantUser({ id, tenantId: alpha.id, name, accessProfileId: 'access-profile-alpha-admin' }): user-alpha-analyst ('Олег Аналитик'), user-alpha-designer ('Мария Дизайнер'), user-alpha-engineer-2 ('Павел Инженер'), user-alpha-lead ('Светлана Лид'). В scripts/seed-dev.ts (блок dataset.users .map/.concat, seed-dev.ts:127-146) дать каждому email (analyst@/designer@/engineer2@/lead@kiss-pm.local), positionId (см. позиции ниже) и password ('analyst12345' и т.п.). Итог: 6 alpha-пользователей с паролями (вместо 2-3). Идемпотентность обеспечена существующим onConflictDoUpdate(target: tenantUsers.id) в seedTenantDataset (seed.ts:403-434).
- **Куда:** packages/test-fixtures/src/index.ts:14-28 (массив users в createDemoTenantDataset — мирорить существующие createTenantUser); scripts/seed-dev.ts:127-146 (dataset.users .map().concat() — мирорить concat user-alpha-engineer)
- **Модель данных:** none — таблицы tenant_users/user_credentials уже есть; роли purely data
- **Разблокирует:** Resources matrix (строки-исполнители), пикеры команды/назначений, аватары на Gantt/Kanban/задачах, фильтры по людям

### Добавить позиции (positions) под новые роли  `[seed · S]`
- **Спека:** В scripts/seed-dev.ts массив dataset.positions (seed-dev.ts:43-56) добавить: position-analyst ('Аналитик'), position-designer ('Дизайнер'), position-lead ('Руководитель направления'), tenantId: 'tenant-alpha'. Привязать positionId новых пользователей из предыдущего пункта к этим id. Идемпотентность: seedTenantDataset уже делает onConflictDoUpdate(target: positions.id) (seed.ts:154-171).
- **Куда:** scripts/seed-dev.ts:43-56 (dataset.positions — мирорить position-project-manager/position-engineer); потребление в seed.ts:154-171
- **Модель данных:** none — таблица positions есть; FK tenant_users.position_id уже существует
- **Разблокирует:** Resources matrix — группировка/легенда по позициям; ресурсная оценка (feasibility) по ролям; Settings → Должности

### Org-структура: tenant_org_nodes + tenant_user_org_placements (functional + project треки)  `[seed · M]`
- **Спека:** Новая идемпотентная функция seedDemoOrgStructure(db) в scripts/seed-dev.ts, вызвать после seedDemoProjectWork. Узлы (tenant_org_nodes, PK [tenantId,id], поля: track, nodeType, name, parentId, sortOrder): functional — direction 'Производство' (org-dir-prod, parentId:null) + departments 'Разработка' (org-dep-dev) и 'Аналитика' (org-dep-analytics, parentId: org-dir-prod); project — direction 'Проектный офис' (org-pdir-pmo, parentId:null) + teams 'Команда A' (org-team-a) и 'Команда B' (org-team-b, parentId: org-pdir-pmo). Соблюсти check-констрейнты: functional→{direction,department}, project→{direction,team}, у direction parentId null. Размещения (tenant_user_org_placements, PK [tenantId,userId,track]): для каждого alpha-пользователя по строке functional (directionId+departmentId+positionId, teamId null) и project (directionId+teamId+positionId, departmentId null), распределив 6 человек по 2 отделам и 2 командам. onConflictDoNothing на узлах и onConflictDoUpdate на размещениях.
- **Куда:** scripts/seed-dev.ts — новая функция рядом с seedDemoPlanningData (seed-dev.ts:473), вызов после строки 157; форма колонок/треков мирорить tenantOrgStructureRepository.ts:144-181 (insert tenantOrgNodes/tenantUserOrgPlacements); схема — schema.ts:1062 (tenantOrgNodes) и schema.ts:1102 (tenantUserOrgPlacements)
- **Модель данных:** none — таблицы tenant_org_nodes (0 строк сейчас) и tenant_user_org_placements уже в схеме
- **Разблокирует:** Settings → Оргструктура (GET /api/tenant/current/org-structure, orgStructureRoutes.ts:178); Resources matrix — группировка строк по направлению/отделу/команде

### 3 портфельных проекта (active / at-risk / closed) с WBS + зависимостями + назначениями + baseline  `[seed · L]`
- **Спека:** Новая функция seedDemoPortfolioProjects(db, createdAt) в scripts/seed-dev.ts, полностью по образцу seedDemoProjectWork+seedDemoPlanningData. Для каждого из 3 проектов (sourceType:'manual', sourceOpportunityId:null — чтобы не плодить opportunities; clientId на seed-клиента): (a) projects (PK [tenantId,id]); (b) projectCalendars + planVersions (мирорить seed-dev.ts:477-505); (c) 5-6 tasks с иерархией WBS (одна summary-задача wbsCode '1' + дети '1.1','1.2', плюс '2','3'), parentTaskId у детей, statusId из task-status-* , taskParticipants (seed-dev.ts:426-448); (d) taskAssignments на разных alpha-исполнителей с unitsPermille/workMinutes (seed-dev.ts:557-627); (e) taskDependencies минимум по одной FS/SS/FF (seed-dev.ts:629-660); (f) projectBaselines + projectBaselineTasks + projectBaselineAssignments >=1 (seed-dev.ts:685-742); (g) projectPositionDemands. Статусы: project-portfolio-active status:'active', deadline > finish последней задачи; project-portfolio-atrisk status:'active', deadline РАНЬШЕ planned_finish последней задачи (триггер deadline-risk, проект остаётся schedulable, capacityService.ts:113); project-portfolio-done status:'closed' + closedAt + activatedAt, все задачи status:'done'/progress:100 (как закрывает retrospectiveRepository.ts:124). Все insert — onConflictDoUpdate(target [tenantId,id]) / onConflictDoNothing.
- **Куда:** scripts/seed-dev.ts — мирорить seedDemoProjectWork (seed-dev.ts:163) и seedDemoPlanningData (seed-dev.ts:473); вызов после seedDemoProjectWork в try-блоке (seed-dev.ts:157). projects.status semantics: capacity/capacityService.ts:113, close → retrospectiveRepository.ts:124
- **Модель данных:** none — все таблицы (projects/tasks/taskDependencies/taskAssignments/projectBaselines*) есть. 'at-risk' — производное состояние (deadline<finish), отдельной колонки статуса не требуется; подтвердить правило deadline-risk у control-движка при wiring
- **Разблокирует:** Projects list (несколько проектов разных состояний), Gantt на реальных данных (WBS-иерархия, связи FS/SS/FF, baseline-оверлей), портфельные дашборды/контур контроля (at-risk сигнал)

### CRM: доп. clients/contacts/products + 3-4 opportunity на разных стадиях воронки  `[seed · M]`
- **Спека:** В dataset (seed-dev.ts) добавить: 2 клиента (client-vektor 'ООО Вектор', client-delta 'АО Дельта') в dataset.clients (seed-dev.ts:57-64); по 1-2 контакта на клиента в dataset.contacts (seed-dev.ts:65-76); 1 доп. продукт в dataset.products (seed-dev.ts:77-98). Затем в новой seedDemoCrmPipeline(transaction, createdAt) (по образцу insert opportunity-demo-crm-intake, seed-dev.ts:166-205) создать 3 opportunities на разных stageId из уже сидируемых стадий (deal-stage-new/qualified/ready, seed-dev.ts:107-126 → проецируются в crm_pipeline_stages): opp-vektor-portal (stageId deal-stage-new, status 'open'), opp-delta-erp (deal-stage-qualified, status 'open'), opp-romashka-expansion (deal-stage-ready, status 'open'), с pipelineId 'tenant-alpha-pipeline-default' (создаётся в seed.ts:291-325), clientName/contactName/title/projectType/plannedStart/plannedFinish/contractValue/plannedHourlyRate/plannedHours/probability/customFieldValues:{}. Опционально opportunityDemands (seed-dev.ts:197-205). onConflictDoUpdate(target [opportunities.tenantId, opportunities.id]).
- **Куда:** scripts/seed-dev.ts:57-98 (dataset.clients/contacts/products — мирорить client-romashka/contact-irina/product-*); seed-dev.ts:166-205 (insert opportunities — мирорить opportunity-demo-crm-intake); pipeline/стадии собираются seed.ts:291-400
- **Модель данных:** none — clients/contacts/products/opportunities + crm_pipeline_* уже в схеме (memory: deal_stages дропнут, стадии живут в crm_pipeline_stages — seed.ts уже проецирует dealStages туда)
- **Разблокирует:** CRM Kanban/воронка (карточки сделок по стадиям), справочники Клиенты/Контакты/Продукты, карточка сделки и переходы по стадиям

### Communications: каналы + участники + conversations + discussion_messages  `[seed · M]`
- **Спека:** Новая seedDemoCommunications(db, createdAt) в scripts/seed-dev.ts. (1) communication_channels (PK [tenantId,id], schema.ts:2421): channel-workspace-general (channelType:'workspace_general', scope null), channel-project-active (channelType:'project_general', scopeEntityType:'project', scopeEntityId: project-portfolio-active), title/description/createdByUserId:'user-alpha-admin'. (2) communication_channel_members (PK [tenantId,channelId,userId], schema.ts:2475, role∈owner/moderator/member) — owner admin + 3-4 member из alpha-юзеров. (3) conversations (PK [tenantId,id], unique [tenantId,entityType,entityId,conversationType], schema.ts:2513): по одной default-беседе на каждый канал (entityType:'communication_channel', entityId=channelId) и одну на проект (entityType:'project', entityId=project-portfolio-active) — те же ключи, что авто-ensure в роуте (collaborationRoutes.ts:75-83), чтобы сообщения легли в каноническую беседу. (4) discussion_messages (PK [tenantId,id], schema.ts:2557): 5-8 сообщений по беседам, authorUserId из участников, body (рус.), metadata:{}, createdAt со сдвигом минут. Все insert идемпотентны: onConflictDoNothing (каналы/беседы — по unique entity-индексу; сообщения — по [tenantId,id]).
- **Куда:** scripts/seed-dev.ts — новая функция; форма колонок мирорить collaborationRepository.ts (insert communicationChannels/conversations/discussionMessages) и роут collaborationRoutes.ts:54-137 (ensureConversation/listDiscussionMessages); схемы schema.ts:2421/2475/2513/2557
- **Модель данных:** none — каналы/беседы/сообщения уже в схеме (0 строк сейчас). Сообщения привязаны к conversations, не напрямую к channel — беседа канала = entityType 'communication_channel'
- **Разблокирует:** Экран Communications/Чат: список каналов, тред сообщений (GET /api/workspace/conversations/:id/messages), члены канала; превью «непрочитанных» при добавлении read-state

### user_notifications (несколько типов) + notification_preferences для admin  `[seed · S]`
- **Спека:** Внутри seedDemoCommunications или отдельной seedDemoNotifications(db, createdAt): 5 строк user_notifications (PK [tenantId,id], schema.ts:2821; notification_type∈mention/assignment_changed/deadline_risk/control_signal/meeting_invite) для userId:'user-alpha-admin', каждая с sourceEntityType/sourceEntityId на реально засидированные сущности (task-* , project-portfolio-atrisk, opportunity, meeting) и route (например '/projects/project-portfolio-atrisk'), title/body рус., часть readAt:null (непрочитанные), часть прочитанные. Доп.: по 1 строке notification_preferences (PK [tenantId,userId,channel,type], schema.ts:2866) channel:'in_app', enabled:true, digestFrequency:'none' на пару типов. onConflictDoNothing.
- **Куда:** scripts/seed-dev.ts — новая функция; схемы schema.ts:2821 (user_notifications) и schema.ts:2866 (notification_preferences); чтение в collaborationRoutes.ts:465 (GET /api/workspace/notifications)
- **Модель данных:** none — user_notifications/notification_preferences уже в схеме (0 строк). source_entity_id — свободный text, FK нет, но ссылаться на реальные id для кликабельных route
- **Разблокирует:** Колокол/инбокс уведомлений (счётчик непрочитанных, типы, переходы по route), экран настроек уведомлений

### 1 meeting + meeting_participants на портфельный проект  `[seed · S]`
- **Спека:** В seedDemoCommunications/seedDemoMeetings: 1 строка meetings (PK [tenantId,id], schema.ts:2897): meeting-portfolio-kickoff, entityType:'project', entityId: project-portfolio-active, title 'Кик-офф проекта', agenda (рус.), scheduledStart/scheduledFinish (finish>start), status:'scheduled', createdByUserId:'user-alpha-admin'. meeting_participants (PK [tenantId,meetingId,userId], schema.ts:2940; role∈organizer/required/optional, response∈pending/accepted/declined): organizer admin (accepted) + 2 required (1 accepted, 1 pending). onConflictDoNothing.
- **Куда:** scripts/seed-dev.ts — новая функция; схемы schema.ts:2897 (meetings) / schema.ts:2940 (meeting_participants); чтение/создание в collaborationRoutes.ts:553-577 (GET/POST /api/workspace/meetings)
- **Модель данных:** none — meetings/meeting_participants уже в схеме (0 строк)
- **Разблокирует:** Панель встреч на карточке проекта, список встреч (GET /api/workspace/meetings), приглашения/ответы участников

### Wiring + идемпотентность под pnpm db:reset:dev  `[seed · S]`
- **Спека:** В scripts/seed-dev.ts: импортировать новые таблицы из '@kiss-pm/persistence' (всё ре-экспортится через export * from './schema', persistence/src/index.ts:25 — доп. экспортов не нужно) — tenantOrgNodes, tenantUserOrgPlacements, communicationChannels, communicationChannelMembers, conversations, discussionMessages, userNotifications, notificationPreferences, meetings, meetingParticipants. В try-блоке (seed-dev.ts:150-161) после seedDemoProjectWork последовательно вызвать seedDemoOrgStructure, seedDemoPortfolioProjects, seedDemoCrmPipeline, seedDemoCommunications/Notifications/Meetings, обновить лог-строку. Все новые insert — строго onConflictDoNothing или onConflictDoUpdate (целевые ключи: композитные PK [tenantId,id] либо unique-индексы как в существующих вызовах seed-dev.ts:228/398/674), фиксированные id и фиксированный createdAt — чтобы повторный db:reset:dev (db:migrate && db:seed:dev, package.json:16-17; без DROP) был полностью идемпотентен и не падал на FK/unique.
- **Куда:** scripts/seed-dev.ts:1-26 (импорты), seed-dev.ts:150-161 (оркестрация try-блока — мирорить вызов seedDemoProjectWork)
- **Модель данных:** none — миграций не требуется; db:migrate остаётся аддитивным
- **Разблокирует:** Стабильный воспроизводимый дев-датасет для всех v3-поверхностей; повторный прогон без ручного сброса БД


---

## Сводка по объёму
Всего пунктов: 27 — S:10 · M:13 · L:4.
