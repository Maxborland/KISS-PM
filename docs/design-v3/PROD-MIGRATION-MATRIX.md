# design-v3 Storybook → Production — Migration Matrix

Источник правды: design-v3 Storybook (33 surfaces, 6 доменов). Прод = те же surfaces на боевом backend.
Единый паттерн: каждый `use-<domain>.ts` свапает mock→real одной строкой (`createXClient({apiOrigin:""})` без fetchImpl); `next.config.ts` проксирует `/api/*`→Hono, cookie-сессия авто.

Статусы: `route-missing-component-ready` (компонент готов, нужен route+транспорт) · `route-missing-component-gaps` (компонент тащит мок-константы/хардкоды — почистить) · `partial`/`integrated`/`verified`.

## delivery (Project Delivery)

**Свап (mock→real):** Primary swap is tiny: in use-planning.ts drop the mock — remove createMockPlanningFetch wiring (lines 40-41) and construct the real client WITHOUT fetchImpl: createPlanningApiClient({ apiOrigin: '' }) (line 44). apiOrigin can stay '' because apps/web/next.config.ts:16-21 rewrites /api/:path* -> ${KISS_PM_API_ORIGIN ?? http://127.0.0.1:4000}/api/:path* — so the client's same-origin /api/... requests are proxied to the Hono API, and the session cookie is sent automatically (credentials default 'include'; no extra auth wiring needed beyond being logged in). TWO extra things are required for full delivery: (1) surfaces hardcode usePlanning(MOCK_PROJECT_ID) — they must receive the real projectId (prop) from the app/projects/[id]/** route; (2) loadCommits (use-planning.ts:172-175) calls the mock-only /planning/commits — it must be replaced by a real client method backed by GET /api/tenant/current/audit-events (planningApiClient.ts:119 fetchProjectAuditEvents) + an adapter to CommitMetaView/CommitsView.

| Surface (component) | Story | Prod route | Status | States | Backend blockers |
|---|---|---|---|---|---|
| ProjectSchedule | Project Delivery/Schedule | `apps/web/src/app/projects/[id]/schedule/page.tsx` | route-missing-component-ready | loading/error/forbidden/success/validation | — |
| ProjectResources | Project Delivery/Resources | `apps/web/src/app/projects/[id]/resources/page.tsx` | route-missing-component-ready | loading/error/forbidden/success/validation | — |
| ProjectBaseline | Project Delivery/Baseline | `apps/web/src/app/projects/[id]/baseline/page.tsx` | route-missing-component-ready | loading/error/forbidden/success/validation | — |
| ProjectScenarios | Project Delivery/Scenarios | `apps/web/src/app/projects/[id]/scenarios/page.tsx` | route-missing-component-ready | loading/error/forbidden/success/validation | — |
| ProjectCalendars | Project Delivery/Calendars | `apps/web/src/app/projects/[id]/calendars/page.tsx` | route-missing-component-ready | loading/error/forbidden/success/validation | — |
| ProjectAssignments | Project Delivery/Assignments | `apps/web/src/app/projects/[id]/assignments/page.tsx` | route-missing-component-ready | loading/error/forbidden/success/validation | — |
| TaskInspector | Project Delivery/Task Inspector | `apps/web/src/app/projects/[id]/inspector/page.tsx (panel; could be nested as app/projects/[id]/tasks/[taskId])` | route-missing-component-ready | loading/error/forbidden/empty/success | Task discussion/messages thread: surface renders EmptyState 'Сообщений пока нет' (task-inspector-surface.tsx:322,410) — read-only, sourced f |
| ProjectOverview | Project Delivery/Overview | `apps/web/src/app/projects/[id]/page.tsx (project index)` | route-missing-component-ready | loading/error/forbidden/empty/success | loadCommits transport gap (NOT a missing backend route): overview reads commits[].summary/actionType/at via the mock-only /planning/commits  |
| ProjectCommits | Project Delivery/Commits | `apps/web/src/app/projects/[id]/commits/page.tsx` | route-missing-component-gaps | loading/error/forbidden/empty/success/validation | CommitsView.latestRevert payload: the surface's revert (commits-surface.tsx:70 applyBatch(inverses)) consumes loadCommits().latestRevert = {<br>CommitMetaView projection: same audit-events -> CommitMetaView adapter as overview is missing (revertible flag has no backend signal). |
| ProjectSettings | Project Delivery/Settings | `apps/web/src/app/projects/[id]/settings/page.tsx` | route-missing-component-ready | loading/error/forbidden/success/validation | — |

_Notes:_ TRANSPORT/AUTH: apps/web/next.config.ts:16-21 already proxies /api/:path* -> ${KISS_PM_API_ORIGIN ?? http://127.0.0.1:4000}/api/:path* and /health. So the production swap keeps apiOrigin:'' in the planning client and just drops fetchImpl (use-planning.ts:44); same-origin proxy means the browser session cookie is sent automatically (createPlanningApiClient credentials default 'include', planningApiClient.ts:26). No per-domain apiOrigin/env wiring is needed beyond setting KISS_PM_API_ORIGIN for th

## workspace

**Свап (mock→real):** Single edit in use-workspace.ts useWorkspaceClient() (lines 22-26): drop the fetchRef/createMockWorkspaceFetch() line and call createWorkspaceClient({ apiOrigin: "" }) with NO fetchImpl, so it uses global fetch against relative /api/workspace/* paths. credentials is already 'include' (workspace-client.ts:99) so the httpOnly session cookie is sent same-origin — no extra auth wiring needed. For a CROSS-origin API: set apiOrigin to the API base URL and add CORS+cookie config; today no apiOrigin/baseURL env exists (providers.tsx has none; lib/api.ts hardcodes relative paths + credentials:'same-origin'), so prod is assumed same-origin behind a reverse proxy.

| Surface (component) | Story | Prod route | Status | States | Backend blockers |
|---|---|---|---|---|---|
| DashboardSurface | Workspace/Dashboard (Дашборд · сводка) | `app/dashboard/page.tsx (candidate home; app/page.tsx currently only shell)` | route-missing-component-ready | loading/error/forbidden (CRM 403 only)/success/empty (inline per-card placeholders) | No tenant-wide aggregate endpoint for meetings/focus-graph/tenant-signals — surface honestly renders a placeholder card (dashboard-surface.t |
| MyWorkSurface | Workspace/My Work (Мои задачи) | `app/my-work/page.tsx` | route-missing-component-gaps | loading/error/empty/ready/(busy/notice on mutation; no forbidden) | No true blocker. Component GAP: imports TASK_STATUSES + WORKSPACE_USERS from mock-workspace-backend (my-work-surface.tsx:12) to build kanban |
| ProjectDetailSurface | Workspace/Project Card (Карточка проекта · задачи и сводка) | `app/projects/[projectId]/page.tsx (param-driven; component currently self-selects via internal switcher)` | route-missing-component-gaps | loading/error/ready/(forbidden reserved but not modeled — mock returns 404 not 403) | No true blocker. Component GAPS: (1) initial selectedId is hardcoded MOCK_PROJECT_ID='proj-prod-portal-r2' imported from mock (project-detai |
| ProjectsListSurface | Workspace/Projects (Проекты · список) | `app/projects/page.tsx` | route-missing-component-ready | loading/error/empty/ready | No true blocker. Minor GAPS: (1) 'Все' tab is a demo toggle — the endpoint returns active-only, so archived/closed are unreachable by design |
| SettingsSurface | Workspace/Settings (Настройки · вкладки) | `app/settings/page.tsx (workspace-level; NOT to be confused with project-scoped delivery/settings -> app/projects/[id]/settings)` | route-missing-component-gaps | loading/error/forbidden (profile: 'требуется вход')/ready/empty (integrations/billing placeholders) | Integrations tab: NO integrations contract in apps/api — surface renders honest EmptyState + disabled demoAction button (settings-surface.ts<br>Billing tab: NO billing/subscription contract in apps/api — honest EmptyState + disabled demoAction (settings-surface.tsx:142-158). TRUE gap<br>Component GAP (not backend): ProfileTab auto-logs-in with hardcoded DEMO_EMAIL/DEMO_PASSWORD on mount (settings-surface.tsx:39-40,82-94) — p |

_Notes:_ apiOrigin/auth supply in prod: there is NO apiOrigin/baseURL/auth context anywhere. apps/web/src/app/providers.tsx only wires React Query + next-themes + Tooltip + Toaster (no auth/origin provider). apps/web/src/lib/api.ts (generic apiFetch) uses RELATIVE paths + credentials:'same-origin' (line 70) but the workspace client does NOT use it — it has its own requestJson with credentials:'include'. Conclusion: production model = web served same-origin with apps/api behind a reverse proxy; session is

## crm

**Свап (mock→real):** In use-crm.ts:35-38 drop the mock: delete fetchRef/createMockCrmFetch() and construct createCrmClient({ apiOrigin: "" }) with NO fetchImpl. Client paths are already absolute (/api/workspace/...), so apiOrigin="" hits same-origin; real fetch + default credentials:'include' carries the session cookie (auth). There is NO NEXT_PUBLIC_API_ORIGIN/env mechanism anywhere in apps/web — same-origin reverse proxy is assumed (apps/web/src/lib/api.ts uses relative paths + credentials:'same-origin'). For a cross-origin API: set apiOrigin to the API URL and ensure CORS allows credentials.

| Surface (component) | Story | Prod route | Status | States | Backend blockers |
|---|---|---|---|---|---|
| ProjectClients | CRM/Clients — "Клиенты" (clients.stories.tsx:11,21) | `apps/web/src/app/crm/clients/page.tsx` | route-missing-component-ready | loading/error/empty/forbidden/success/validation | — |
| ProjectContacts | CRM/Contacts — "Контакты" (contacts.stories.tsx:11,21) | `apps/web/src/app/crm/contacts/page.tsx` | route-missing-component-ready | loading/error/empty/forbidden/success/validation | — |
| ProjectProducts | CRM/Products — "Продукты" (products.stories.tsx:11,21) | `apps/web/src/app/crm/products/page.tsx` | route-missing-component-ready | loading/error/empty/forbidden/success/validation | — |
| ProjectDeals | CRM/Deals — "Сделки · воронка" (deals.stories.tsx:14,24) | `apps/web/src/app/crm/deals/page.tsx` | route-missing-component-ready | loading/error/forbidden/success/validation | — |
| DealCard | CRM/Deal Card — "Карточка сделки" (deal-card.stories.tsx:15,25) | `apps/web/src/app/crm/deals/[id]/page.tsx` | route-missing-component-gaps | loading/error/forbidden/success/validation | — |

_Notes:_ AUTH/SESSION: cookie session via client default credentials:'include' (crm-client.ts:129); whole-section 403 → useCrm 'forbidden' status → surfaces render SurfaceState forbidden (use-crm.ts:74-78). APIORIGIN: no env mechanism in apps/web — swap is in-code (use-crm.ts:38, apiOrigin:''); same-origin reverse proxy assumed so browser /api/workspace/* reaches apps/api (lib/api.ts confirms relative-path + same-origin pattern); cross-origin API would need apiOrigin set + CORS-with-credentials. SHARED L

## communications

**Свап (mock→real):** Single shared edit in use-comms.ts:109: change `createCommsClient({ apiOrigin: "", fetchImpl: createMockCommsFetch() })` → `createCommsClient({ apiOrigin: "" })` and drop the `import { createMockCommsFetch }` (l.35). apiOrigin stays "" (same-origin) because apps/web/next.config.ts:16-24 rewrites /api/:path* → ${KISS_PM_API_ORIGIN ?? http://127.0.0.1:4000}/api/:path*; credentials already defaults to 'include' so the session cookie flows same-origin; x-kiss-pm-action header already sent. No per-surface change. Same pattern as admin/auth/crm clients (all apiOrigin:'').

| Surface (component) | Story | Prod route | Status | States | Backend blockers |
|---|---|---|---|---|---|
| ChatSurface | Communications/Chat — "Чат · беседы проекта" | `apps/web/src/app/communications/chat/page.tsx (or app/communications/page.tsx as index)` | route-missing-component-ready | loading/error/forbidden/empty (nested: 'Нет бесед' / 'Сообщений пока нет')/success/ready/validation (composer requires body or sticker) | No unpin route (POST .../pin is one-way; pin toggle-off not supported by API — surface only offers Pin, matches contract, not a blocker)<br>No realtime/SSE push endpoint — feed updates on action (refetch); acceptable for migration, no live delivery |
| ChannelsSurface | Communications/Channels — "Каналы · список и детали" | `apps/web/src/app/communications/channels/page.tsx` | route-missing-component-ready | loading/error/forbidden/empty (nested: 'Нет каналов')/success/ready/inline detail loading (fallback from list)/validation (create/edit dialogs) | — |
| CallsSurface | Communications/Calls — "Звонки · комнаты и сессии" | `apps/web/src/app/communications/calls/page.tsx` | route-missing-component-ready | loading/error/forbidden/empty (top-level: 'Нет комнат')/success/ready/detail forbidden/error/loading/inline error-code banner (e.g. video_provider_misconfigured)/validation (create dialog) | No WebRTC media — by design (surface is honest 'Прототип': join-token only, real Jitsi/LiveKit connection only in prod deploy). Not a contra<br>join-token gated by deploy video provider config (videoProvider.ts) — returns 409 video_provider_misconfigured if room.provider != configure |
| MeetingsSurface | Communications/Meetings — "Встречи · список и детали" | `apps/web/src/app/communications/meetings/page.tsx` | route-missing-component-gaps | loading/error/forbidden/empty (nested: 'Встреч пока нет')/success/ready (list + create + write-mutations real)/validation (create dialog finish>start)/DETAIL READ FAKED (seed+optimistic, not server) | GET /api/workspace/meetings/:meetingId (meeting DETAIL) — ABSENT. No GET returns participants/notes/external-links/action-items (verified: m<br>Client gap that follows: comms-client.ts has NO getMeeting()/listMeetingNotes()/listMeetingParticipants() etc. — must be added once the GET <br>No action-item status mutation route (surface notes status always 'open') |
| NotificationsSurface (also exports NotificationsPrefs) | Communications/Notifications — "Уведомления · лента и настройки" | `apps/web/src/app/communications/notifications/page.tsx` | route-missing-component-ready | loading/error/forbidden/empty (filter-aware nested: unread/read/all)/success/ready/prefs loading/error/forbidden/success/validation (minimal) | No bulk mark-all-read endpoint — surface honestly fans out N× POST /:id/read (notifications-surface.tsx:124-136). Workaround, not a hard blo<br>No realtime/SSE push for new notifications — feed refetches on action |

_Notes:_ TRANSPORT/AUTH (domain-wide, no blocker): comms client default credentials:'include' + header x-kiss-pm-action:'same-origin' (comms-client.ts:397-398) is production-ready. Production apiOrigin is supplied NOT via env in the web app code but via apps/web/next.config.ts:5,16-24 rewrites — /api/:path* and /health are proxied to KISS_PM_API_ORIGIN (default http://127.0.0.1:4000). So apiOrigin stays "" (same-origin) and the lone swap is dropping fetchImpl in use-comms.ts:109 (one shared edit covers a

## admin

**Свап (mock→real):** In use-admin.ts:30-34 drop the mock entirely: delete fetchRef/createMockAdminFetch() and create the client as `createAdminClient({ apiOrigin: process.env.NEXT_PUBLIC_API_ORIGIN ?? '' })` — no fetchImpl (uses global fetch), apiOrigin '' = same-origin relative paths matching lib/api.ts. Auth: client credentials already default to 'include', so the browser session cookie is sent automatically; backend resolves the actor via getSessionActorFromHeaders(cookie) (workspaceUserRoutes.ts:27-30) — user must be logged in (session cookie present), no bearer/token header needed. No response-shape changes: every real route already returns the exact keys the client reads (accessRoles/accessProfile/accessRole/{status:'deleted'}/users/user/positions).

| Surface (component) | Story | Prod route | Status | States | Backend blockers |
|---|---|---|---|---|---|
| AdminUsersSurface | Admin/Users — "Пользователи" (users.stories.tsx) | `apps/web/src/app/admin/users/page.tsx` | route-missing-component-ready | loading/error/empty/success/ready/validation (mutation errors shown inline via notice + adminErr code mapping, not via SurfaceState) | — |
| AdminRolesSurface | Admin/Roles — "Роли" (roles.stories.tsx) | `apps/web/src/app/admin/roles/page.tsx` | route-missing-component-ready | loading/error/empty/success/ready/validation (mutation errors shown inline via notice + adminErr code mapping, not via SurfaceState) | — |

_Notes:_ Auth is cookie-session based: every admin route calls getSessionActorFromHeaders(context.req.header('cookie')) and returns 401 session_required (or dev_session_required for POST access-profiles) when absent; RBAC enforced via @kiss-pm/access-control (canRead/ManageTenantUsers, access-profile guards) → 403 {error:reason}. All 8 endpoints the admin client uses EXIST and are wired in apps/api/src/app.ts (registerAccessRoleRoutes :254, registerWorkspaceUserRoutes :278, registerPositionRoutes :279) a

## auth

**Свап (mock→real):** In useAuthClient() (use-auth.ts:36-42) the only change is lines 37-40: drop createMockAuthFetch() and the fetchImpl, and pass a real origin — createAuthClient({ apiOrigin: process.env.NEXT_PUBLIC_API_ORIGIN ?? '' }). With apiOrigin:'' the client hits relative /api/* (requires same-origin reverse-proxy / Next rewrite to apps/api); otherwise set the API origin string + CORS-with-credentials. No other code changes: the HttpOnly session cookie is already carried by credentials:'include' (auth-client.ts:89) and CSRF header logic (x-kiss-pm-action, login exception) is already implemented (auth-client.ts:91-101).

| Surface (component) | Story | Prod route | Status | States | Backend blockers |
|---|---|---|---|---|---|
| LoginSurface | Auth/Login | `apps/web/src/app/(auth)/login/page.tsx (or app/login/page.tsx) — surface ships its own AuthShell, mount standalone` | route-missing-component-ready | loading/error/success/validation | — |
| RegisterSurface | Auth/Register | `apps/web/src/app/(auth)/register/page.tsx` | route-missing-component-ready | loading/success/validation | — |
| ResetRequestSurface | Auth/Password Reset (Запрос) | `apps/web/src/app/(auth)/password-reset/page.tsx` | route-missing-component-ready | loading/success/validation | NON-BLOCKING: real /password-reset/request returns only {status:'ok'} (no devToken — real flow emails the token via EmailProvider). Surface  |
| ResetConfirmSurface | Auth/Password Reset (Подтверждение) | `apps/web/src/app/(auth)/password-reset/confirm/page.tsx (read ?token= via searchParams → token prop)` | route-missing-component-ready | loading/success/validation | — |
| ProfileSurface (also exports ProfileContent for reuse in settings tab) | Auth/Profile | `apps/web/src/app/profile/page.tsx — surface renders inside its own WorkspaceShell` | route-missing-component-gaps | loading/error/forbidden/success/validation | — |
| AvatarMenuSurface | Auth/Avatar | `n/a — SHELL, not a standalone route: the live avatar menu belongs in the topbar of delivery/ui/workspace-shell (WorkspaceShell). This surface is a Storybook showcase of that menu.` | route-missing-component-gaps | loading/error/forbidden/success | NON-BLOCKING: 'Активные сессии'/active-devices list has NO backend contract — surface already shows an honest placeholder (avatar-menu-surfa |

_Notes:_ AUTH/SESSION: cookie-based. POST /api/auth/login (authRoutes.ts:33) and /api/auth/register (authRegistrationRoutes.ts:49) set an HttpOnly session cookie (buildSessionCookieHeader); GET /api/auth/me (authRoutes.ts:119) reads it and returns 401 {error:'session_required'} when absent. The client already uses credentials:'include' (auth-client.ts:89), so the only swap is apiOrigin + dropping fetchImpl. State is read from me()/login responses, never document.cookie (HttpOnly) — correct for prod.  CSR

