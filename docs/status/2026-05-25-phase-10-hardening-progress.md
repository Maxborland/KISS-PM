# Phase 10 backend hardening progress — 2026-05-25

## Scope

Этот ledger фиксирует уже закрытые backend hardening gaps в ветке `codex/phase10-backend-hardening`. Он дополняет capability inventory и нужен, чтобы Phase 10 не стала неявным набором разрозненных фиксов.

## Closed gaps

| Area | Gap | Resolution | Evidence |
|------|-----|------------|----------|
| Operations readiness | Не было разделения liveness/readiness и provider checks | Добавлены `/health/live`, `/api/health/live`, `/health/ready`, `/api/health/ready`; readiness проверяет DB/storage | `pnpm vitest run apps/api/src/app.test.ts`, `pnpm typecheck` |
| Operations readiness | Production мог стартовать без `DATABASE_URL` и уйти в in-memory datasource | Server startup config now fails closed for production without Postgres before `createApp` can fall back to demo persistence | `pnpm vitest run apps/api/src/serverReadiness.test.ts apps/api/src/app.test.ts` |
| Operations readiness | DB readiness делал только `select 1`, поэтому пустая/unmigrated база могла считаться ready | Readiness now verifies the expected latest migration tag in `kiss_pm_migrations` after connectivity check | `pnpm vitest run apps/api/src/serverReadiness.test.ts`, `pnpm typecheck` |
| Operations readiness | Production local storage мог писать в default `.kiss-pm-storage` без явного root | `KISS_PM_STORAGE_LOCAL_ROOT` обязателен для local storage в production | `pnpm vitest run apps/api/src/storageProvider.test.ts apps/api/src/serverReadiness.test.ts` |
| Security headers | Public `/health*` routes не получали baseline security/no-store headers | Baseline headers применяются ко всем API app routes | `pnpm vitest run apps/api/src/app.test.ts apps/api/src/requestSecurity.test.ts` |
| Attachment privacy | File download response не задавал explicit no-store cache policy | Download response задает `Cache-Control: no-store, private` | `pnpm vitest run --config vitest.db.config.ts apps/api/src/attachmentRoutes.db.test.ts` |
| External references | IPv6 unspecified literals `[::]` / `[0:...:0]` and `.localhost` subdomains не отклонялись как unsafe hosts | URL validation rejects IPv6 unspecified hosts and local-use `.localhost` names | `pnpm vitest run apps/api/src/attachmentValidation.test.ts apps/api/src/storageProvider.test.ts` |
| KPI expression safety | Constrained KPI AST валидировал finite листья, но арифметика могла вернуть `Infinity` | KPI expression arithmetic normalizes operation results through finite guard | `pnpm vitest run packages/domain/src/control/controlEngine.test.ts`, `pnpm typecheck` |
| Control action apply | После planning lock повторно проверялось наличие `planDelta`, но не пустой command list | Locked action recheck rejects empty planDelta before preview/apply/version increment | `pnpm vitest run apps/api/src/app.test.ts packages/domain/src/control/controlEngine.test.ts`, `pnpm typecheck` |
| Control surface lifecycle | Archived control surface можно было восстановить через publish/rollback paths, including a possible archive race between read and update | Publish/rollback reject archived surfaces; persistence update predicates now atomically require non-archived status | `pnpm vitest run apps/api/src/controlSurfaceRoutes.test.ts packages/persistence/src/controlSurfaceRepository.db.test.ts packages/persistence/src/migration.test.ts`, `pnpm typecheck` |
| Closure denied audit | Lesson creation and template improvement apply returned 403 without denied audit | Retrospective lesson and template improvement denied paths write closure audit events | `pnpm vitest run apps/api/src/retrospectiveRoutes.test.ts`, `pnpm typecheck` |
| Release-like smoke | Не было DB smoke, который проходит полный backend management loop | Добавлен сквозной DB smoke: auth → planning apply → control evaluate → closure close → audit read | `pnpm vitest run --config vitest.db.config.ts apps/api/src/backendManagementLoop.db.test.ts`, `pnpm typecheck` |
| Planning auto-solver permissions | Solver run proposals could expose assignment/allocation deltas to plan managers without resource-manage permission | Auto-solver run create/read now require resource management permission before persisted proposal data is returned | `pnpm vitest run --config vitest.db.config.ts apps/api/src/planningRoutes.db.test.ts`, `pnpm typecheck` |
| Capacity invalidation | Closing a project changed capacity-committed status but left tenant capacity cache warm until TTL | Project closure now invalidates tenant capacity cache so closed project load disappears immediately | `pnpm vitest run --config vitest.db.config.ts apps/api/src/capacityRoutes.db.test.ts`, `pnpm typecheck` |
| Control action preview permissions | Management action preview returned persisted `planDelta` with only execute/control-read permissions, before action-specific permission checks | Preview now runs the same action-specific permission gate as apply, writes denied audit/execution on refusal, and control read-model requires project plan read | `pnpm vitest run apps/api/src/app.test.ts`, `pnpm typecheck` |
| Control surface action binding safety | Action permission arrays accepted non-string/blank entries as long as the mandatory permissions were present | Control surface validation now rejects malformed permission array entries before publish | `pnpm vitest run packages/domain/src/controlSurfaces/validation.test.ts apps/api/src/controlSurfaceRoutes.test.ts`, `pnpm typecheck` |
| Closure snapshot read boundary | Project close built and returned a plan-derived closure snapshot without requiring project plan read permission | Close permission composition now requires `tenant.project_plan.read` before snapshot construction, and denied close remains audited | `pnpm vitest run apps/api/src/retrospectiveRoutes.test.ts`, `pnpm typecheck` |
| Planning auto-solver apply audit | Persisted solver apply returned stale plan/precondition 409 responses without an audit trail | Solver apply now writes conflict/precondition audit events before returning governed 409 outcomes | `pnpm vitest run apps/api/src/planningAutoSolverRoutes.test.ts`, `pnpm typecheck` |
| KPI definition read exposure | Control read-model returned KPI definitions without requiring the dedicated KPI definition read permission | Control read-model now requires `tenant.kpi_definitions.read` before loading definitions/evaluations/signals | `pnpm vitest run apps/api/src/app.test.ts`, `pnpm typecheck` |
| Control surface published action exposure | Published control surface read-model returned actions even when the actor lacked action-specific permissions | Non-builder published read-model now filters actions by actor permissions while builder state remains complete | `pnpm vitest run apps/api/src/controlSurfaceRoutes.test.ts`, `pnpm typecheck` |
| Control surface required-permission exposure | Published surfaces were visible to non-builder readers even when their definition-level required permissions were missing | Non-builder published read-model now hides surfaces unless definition `requiredPermissions` are all granted | `pnpm vitest run apps/api/src/controlSurfaceRoutes.test.ts`, `pnpm typecheck` |
| KPI definition mutation denied audit | KPI definition upsert denied by missing manage permission returned 403 without an audit trail | Denied KPI definition upserts now write safe metadata-only control audit events before returning 403 | `pnpm vitest run apps/api/src/app.test.ts`, `pnpm typecheck` |
| Management action route-level denied audit | Management action preview/apply route-level permission failures returned 403 before loading candidates without audit | Preview/apply now audit safe route metadata for execute/read permission denials before loading action candidates | `pnpm vitest run apps/api/src/app.test.ts`, `pnpm typecheck` |
| Planning auto-solver apply rejection audit | Expired, already-applied, and payload-hash-mismatch solver apply rejections returned 409 without audit | Solver apply now writes rejection-specific audit events before returning governed 409 outcomes | `pnpm vitest run apps/api/src/planningAutoSolverRoutes.test.ts`, `pnpm typecheck` |
| Planning auto-solver proposal rejection audit | Missing proposal ids and accepted-overload proposals without risk reason returned errors without audit | Solver apply now audits missing proposal and accepted-risk-reason precondition failures before returning 404/409 | `pnpm vitest run apps/api/src/planningAutoSolverRoutes.test.ts`, `pnpm typecheck` |
| Planning auto-solver route-level denied audit | Solver run create/read/apply route-level permission denials returned 403 without audit | Solver create/read/apply now write safe route-metadata denied audit before exposing or applying persisted proposals | `pnpm vitest run apps/api/src/planningAutoSolverRoutes.test.ts`, `pnpm typecheck` |
| Closure retry/conflict audit | Close-project race outcomes mapped to stable 404/409 but left no audit trail | Closure close now audits not-found/closability race failures without writing success `project.closed` audit | `pnpm vitest run apps/api/src/retrospectiveRoutes.test.ts`, `pnpm typecheck` |
| Template improvement apply retry audit | Retrying or mis-targeting template improvement apply returned 404/409 without audit | Template improvement apply now audits not-found/not-proposed/already-applied failures without duplicate success audit | `pnpm vitest run apps/api/src/retrospectiveRoutes.test.ts`, `pnpm typecheck` |
| Closure read/preview denied audit | Closure read and preview denied paths returned 403 without audit | Closure read/preview now write best-effort denied audit before returning 403 | `pnpm vitest run apps/api/src/retrospectiveRoutes.test.ts`, `pnpm typecheck` |
| Control surface publish failure audit | Publish blocked/archived conflicts returned 409 without mutation audit | Publish conflicts now write control surface failure audit with validation/reason metadata | `pnpm vitest run apps/api/src/controlSurfaceRoutes.test.ts`, `pnpm typecheck` |
| Control surface rollback/archive failure audit | Rollback/archive not-found or archived conflicts returned errors without mutation audit | Rollback/archive failure paths now write control surface failure audit with reason metadata | `pnpm vitest run apps/api/src/controlSurfaceRoutes.test.ts`, `pnpm typecheck` |
| Control surface read denied audit | Control surface list/detail/preset denied paths returned 403 without audit | Control surface read/preset denials now write best-effort denied audit | `pnpm vitest run apps/api/src/controlSurfaceRoutes.test.ts`, `pnpm typecheck` |
| Storage/search security sweep | Before Phase 10, route-adjacent risky sinks needed explicit review against security guidance | Redirect/eval/shell patterns are absent; filesystem access is limited to storage provider/migration/test paths; local storage resolves keys under configured root; outbound runtime `fetch` is limited to env-configured S3 provider; external references are never server-fetched | `rg "redirect\\(|location\\(" apps/api packages -n`, `rg "new Function|eval\\(" apps/api packages -n`, `rg "child_process|exec\\(|spawn\\(" apps/api packages -n`, `rg "sendFile|createReadStream|readFile|writeFile|unlink\\(|rm\\(" apps/api packages -n`, `rg "fetch\\(|axios|http\\.request|https\\.request" apps/api packages -n`, `pnpm test`, `pnpm test:db`, `pnpm typecheck` |
| Capacity lifecycle invalidation review | Less common capacity-committed lifecycle statuses could leave cache stale if a public transition existed outside activate/close | Reviewed lifecycle entrypoints: activation, close, planning apply, solver apply, task assignment/status updates, absences, calendars, org/position/user changes invalidate capacity cache; `paused/cancelled` have no public project transition route in this backend slice | `rg "paused|cancelled|activateProjectDraft|closeProject|invalidateCapacity" apps/api packages -n`, `codegraph_context: Capacity cache invalidation and capacity-committed project lifecycle statuses`, `pnpm test:db` |
| Planning / solver persistence mapping review | Low-level persistence failures after governed solver apply could be over-mapped and hide real DB bugs | Reviewed solver apply flow: known stale/precondition/proposal/hash/expiry/business conflicts now return stable audited responses; unknown persistence errors remain un-mapped 500s so data-source bugs are not disguised as business outcomes | `codegraph_context: Planning auto solver apply low-level persistence failure mapping`, `pnpm vitest run apps/api/src/planningAutoSolverRoutes.test.ts`, `pnpm test:db` |
| KPI / action engine persistence review | Action execution persistence edge cases needed a final pass after route-level denied audit fixes | Reviewed management action flow: route-level denials are audited before candidate loading; governed preview/apply paths keep transaction rollback semantics; unknown persistence failures remain 500 instead of partial success or false business conflicts | `codegraph_context: Management action execution persistence edge cases`, `pnpm vitest run apps/api/src/app.test.ts`, `pnpm test` |
| Control surface persistence mapping review | Publish/rollback/archive low-level persistence failures needed a final pass after read denied audit symmetry | Reviewed control surface mutation paths: not-found/archived/validation conflicts now have audited stable responses; unknown repository failures remain 500 so persistence bugs surface during verification | `codegraph_context: Control surface low-level persistence failure mapping`, `pnpm vitest run apps/api/src/controlSurfaceRoutes.test.ts`, `pnpm test` |
| Closure auxiliary persistence mapping review | Closure auxiliary commands needed a final pass beyond close-project race handling | Reviewed close, lesson, template improvement preview/apply/retry paths: known denied/conflict/not-found/retry outcomes now audit and map stably; unknown persistence failures are not converted into business errors | `codegraph_context: Closure auxiliary command persistence failure mapping`, `pnpm vitest run apps/api/src/retrospectiveRoutes.test.ts`, `pnpm test` |
| Release-like smoke | Phase 10 needs a backend loop gate that stays in the verification set instead of becoming a one-off check | Backend management loop DB smoke remains part of broad verification; expand it only when a new backend phase adds a mandatory loop step | `pnpm test:db`, `apps/api/src/backendManagementLoop.db.test.ts` |
| Session cookie production default | Production sessions depended on remembering `KISS_PM_SECURE_COOKIES=true` to set the `Secure` attribute | `NODE_ENV=production` now defaults session cookies to `Secure`, with explicit `KISS_PM_SECURE_COOKIES=false` opt-out for unusual non-TLS self-hosted deployments | `pnpm vitest run apps/api/src/authSession.test.ts apps/api/src/app.test.ts`, `pnpm typecheck` |
| Backend dependency security audit | SCA reported moderate transitive backend/tooling advisories for `esbuild`/`uuid`, while a separate `apps__web>happy-dom` finding remains out of this backend-only slice | Backend security audit gate filters frontend-only package paths and pins patched backend/tooling transitive dependencies through pnpm overrides | `pnpm security:audit`, `pnpm vitest run apps/api/src/authSession.test.ts apps/api/src/app.test.ts apps/api/src/attachmentValidation.test.ts apps/api/src/storageProvider.test.ts`, `pnpm typecheck` |
| Security audit gate | Dependency security audit was only an ad-hoc command and could drift out of release verification | Added root `security:audit` script that runs `pnpm audit --audit-level moderate`; current lockfile passes with no known vulnerabilities | `pnpm security:audit` |
| Static security scan gate | Secret and backend dangerous-sink scans were manual and could be forgotten between phases | Added root `security:scan` and combined `security:check`; scan blocks committed env files, common secret token shapes, server dynamic execution, insecure HTTP parser config, sensitive backend logging, and broad CORS in API runtime code | `pnpm security:check` |
| Security CI gate | Security checks were available locally but not enforced on PR/push | Added GitHub Actions security workflow that installs with frozen lockfile and runs `pnpm security:check` on app/package/script/lockfile changes | `.github/workflows/security.yml`, `pnpm security:check` |
| Security CI least privilege | Security workflow used default token behavior for a read-only audit job | Workflow now grants only `contents: read` and disables checkout credential persistence | `.github/workflows/security.yml` |
| Supply-chain secret scan | Static scan covered common app tokens but did not explicitly catch npm registry auth tokens | `security:scan` now blocks committed `_authToken`/`NPM_TOKEN` style registry credentials | `pnpm security:check` |
| API browser isolation headers | Header baseline covered CSP/frame/nosniff/referrer but not modern cross-origin isolation or legacy Flash/Acrobat cross-domain policy controls | API responses now include `Cross-Origin-Opener-Policy`, `Cross-Origin-Resource-Policy`, and `X-Permitted-Cross-Domain-Policies` | `pnpm vitest run apps/api/src/requestSecurity.test.ts`, `pnpm typecheck` |
| JSON body content-type enforcement | JSON command endpoints parsed any non-empty request body as JSON even when the request was `text/plain` or another non-JSON media type | Shared JSON body reader now accepts only `application/json` and structured `+json` media types for non-empty bodies, returning stable `unsupported_media_type` otherwise | `pnpm vitest run apps/api/src/jsonBody.test.ts apps/api/src/app.test.ts apps/api/src/authRateLimit.test.ts`, `pnpm typecheck` |
| Session cookie priority | Session cookies had secure attributes but no explicit browser eviction priority | Session and expired-session cookies now include `Priority=High` alongside `HttpOnly`, `Path=/`, `SameSite=Lax`, bounded lifetime, and production `Secure` default | `pnpm vitest run apps/api/src/authSession.test.ts`, `pnpm typecheck` |
| API HTTP DoS baseline | Self-hosted API server relied on platform defaults for request/header/keep-alive limits | API startup now sets bounded `requestTimeout`, `headersTimeout`, `keepAliveTimeout`, and `maxHeadersCount` on the Node HTTP server | `pnpm vitest run apps/api/src/serverSecurity.test.ts`, `pnpm typecheck` |
| Session lookup CPU DoS | Cookie session lookup used synchronous `scrypt` on every authenticated request before DB lookup | Session token storage now hashes already-random 256-bit bearer tokens with a domain-separated SHA-256 digest, keeping password KDF only for passwords | `pnpm vitest run packages/persistence/src/auth.test.ts apps/api/src/app.test.ts`, `pnpm typecheck` |
| Session token boundary validation | Session cookie extraction accepted arbitrary token strings and could pass malformed values into session hash lookup/delete paths | Session cookie lookup now accepts only the generated 64-hex token shape, treating malformed cookies as absent before persistence lookup or logout deletion | `pnpm vitest run apps/api/src/authSession.test.ts apps/api/src/app.test.ts apps/api/src/controlSurfaceRoutes.test.ts apps/api/src/retrospectiveRoutes.test.ts apps/api/src/planningAutoSolverRoutes.test.ts apps/api/src/searchRoutes.test.ts` |
| Security-sensitive user updates | Existing session rows could remain stored after user email, access role, or active-status changes | Workspace user updates now revoke that user's persisted sessions after security-sensitive identity/access changes | `pnpm vitest run --config vitest.db.config.ts apps/api/src/app.db.test.ts`, `pnpm typecheck` |
| Login timing enumeration | Unknown login emails skipped password KDF work, creating a timing signal compared with existing emails and wrong passwords | Login now verifies unknown emails against a stable dummy password record before returning `invalid_credentials`, preserving rate-limit behavior while reducing account-existence timing leakage | `pnpm vitest run apps/api/src/app.test.ts`, `pnpm typecheck` |
| Login spray abuse control | Random-email login sprays could bypass per-email throttling and grow in-memory rate-limit buckets until the window expired | Auth rate limiter now has bounded email/IP bucket maps plus a global failed-login bucket for broad spray attempts | `pnpm vitest run apps/api/src/authRateLimit.test.ts apps/api/src/app.test.ts`, `pnpm typecheck` |
| API origin configuration hardening | Trusted mutation origins accepted raw env strings, allowing malformed schemes, credentials, paths, query strings, or fragments to silently weaken or break security boundaries | API trusted-origin allowlist now canonicalizes to origin-only `http(s)` values and ignores unsafe origin shapes fail-closed | `pnpm vitest run apps/api/src/requestSecurity.test.ts`, `pnpm typecheck` |
| API route error detail exposure | App-level route exceptions needed integration evidence that unknown exception messages are not serialized to clients | Hono `onError` path now has a regression test proving unknown route errors return only `{ error: "internal_error" }` | `pnpm vitest run apps/api/src/app.test.ts`, `pnpm typecheck` |
| Sensitive runtime logging gate | Static scan blocked dangerous sinks but did not catch accidental console logging of headers/cookies/tokens/secrets | `security:scan` now fails on runtime `console.*` calls that include sensitive header/session/secret terms in API/package backend source files | `pnpm security:scan`, `pnpm security:check` |
| S3 storage outbound DoS bound | S3-compatible storage provider used outbound `fetch` without an explicit timeout, allowing a stalled storage endpoint to hold API work indefinitely | S3 storage requests now use `AbortSignal.timeout` with a bounded `KISS_PM_STORAGE_S3_TIMEOUT_MS` override, defaulting to 15s and rejecting unsafe values | `pnpm vitest run apps/api/src/storageProvider.test.ts`, `pnpm typecheck` |
| S3 storage config hardening | S3 endpoint and bucket env values were interpolated into outbound URLs without fail-closed shape validation | S3 endpoint config is now origin-only `http(s)` without credentials/path/query/hash, and bucket config rejects path/control-like forms before any request can be built | `pnpm vitest run apps/api/src/storageProvider.test.ts`, `pnpm typecheck` |
| Attachment storage key hardening | Upload display names and storage key segments relied on upstream IDs/provider root checks for complete path-shape safety | Attachment filenames now drop leading traversal/hidden-file prefixes, normalize Windows reserved device names, and `buildStorageKey` sanitizes every tenant/asset/file segment before provider calls | `pnpm vitest run apps/api/src/attachmentValidation.test.ts apps/api/src/storageProvider.test.ts apps/api/src/attachmentRoutes.test.ts`, `pnpm typecheck` |
| Attachment metadata sanitization | External reference metadata accepted arbitrary object graphs, prototype-pollution keys, and non-JSON values before persistence/search reuse | Metadata parsing now returns a sanitized bounded JSON clone, rejects `__proto__`/`prototype`/`constructor`, non-finite numbers, class/date/function-like values, excessive depth, arrays, keys, strings, and total serialized size | `pnpm vitest run apps/api/src/attachmentValidation.test.ts apps/api/src/attachmentRoutes.test.ts apps/api/src/searchRoutes.test.ts`, `pnpm typecheck` |
| Attachment identifier validation | Attachment route inputs still accepted path-like/control-bearing `entityId`, `externalId`, and `relationType` values, and multipart `entityId` could stringify a file object | Attachment route boundaries now parse bounded string identifiers explicitly, reject traversal/control/path-like shapes, keep `relationType` on a strict identifier allowlist, and return stable 400 errors before persistence or permission lookup | `pnpm vitest run apps/api/src/attachmentValidation.test.ts apps/api/src/attachmentRoutes.test.ts`, `pnpm vitest run --config vitest.db.config.ts apps/api/src/attachmentRoutes.db.test.ts` |
| Attachment route id and download denial audit | Download/delete route params accepted unbounded attachment ids before persistence lookup, and download read-denials returned 403 without attachment audit | Attachment route ids now require bounded `attachment-*`/legacy `attachment-task-*` shapes before persistence, and download read-denials write safe `attachment.denied` audit events | `pnpm vitest run apps/api/src/attachmentValidation.test.ts apps/api/src/attachmentRoutes.test.ts`, `pnpm vitest run --config vitest.db.config.ts apps/api/src/attachmentRoutes.db.test.ts` |
| Project work route id validation | Project/task/status route params accepted unbounded or path-like ids before permission and persistence lookup | Project work route ids now pass through bounded safe identifier parsing before body parsing, preflight, or repository access, with stable 400 errors | `pnpm vitest run apps/api/src/projectWorkParsers.test.ts`, `pnpm vitest run --config vitest.db.config.ts apps/api/src/projectWorkRoutes.db.test.ts` |
| Control action route id validation | Control/action route params accepted unbounded or path-like project/signal/action ids before session, permission and persistence lookup | Control read/evaluate/action/status/corrective-action routes now use shared bounded route-id parsers with stable 400 errors before session or data-source access | `pnpm vitest run apps/api/src/routeParamParsers.test.ts apps/api/src/projectWorkParsers.test.ts apps/api/src/app.test.ts`, `pnpm typecheck` |
| Control surface route id validation | Control surface governance routes accepted unbounded or path-like `surfaceId` before session, permission, audit, and persistence lookup | Control surface detail/preview/publish/rollback/archive routes now use the shared bounded route-id parser with stable `invalid_control_surface_id` responses before session or data-source access | `pnpm vitest run apps/api/src/routeParamParsers.test.ts apps/api/src/controlSurfaceRoutes.test.ts`, `pnpm typecheck` |
| Access role id validation | Access-role route/body identifiers accepted unbounded or path-like ids before identity/permission administration persistence | Access-role route params and new access profile body ids now use bounded allowlist validation with stable route/body errors before persistence | `pnpm vitest run apps/api/src/routeParamParsers.test.ts apps/api/src/workspaceParsers.test.ts apps/api/src/app.test.ts`, `pnpm typecheck` |
| Workspace user and position id validation | Workspace user/position route and body identifiers accepted unbounded or path-like ids before identity/org persistence and audit | User and position route params plus body `id`/`accessProfileId`/`positionId` values now use bounded allowlist validation, while PATCH still supports clearing position with an empty value | `pnpm vitest run apps/api/src/routeParamParsers.test.ts apps/api/src/workspaceParsers.test.ts apps/api/src/app.test.ts`, `pnpm typecheck` |
| Workspace admin payload bounds | Workspace access-profile, user, and position payloads accepted unbounded or control-bearing names/contact fields/passwords/descriptions before identity/org persistence | Workspace parsers now bound and sanitize access profile names, user email/name/contact/password fields, and position name/description before persistence or audit reuse | `pnpm vitest run apps/api/src/workspaceParsers.test.ts apps/api/src/app.test.ts` |
| Workspace config route id validation | Workspace custom-field/template route identifiers accepted unbounded or path-like ids before session and configuration persistence lookup | Custom-field and project-template PATCH route params now use bounded workspace-config id parsing with stable 400 errors before session or data-source access | `pnpm vitest run apps/api/src/routeParamParsers.test.ts apps/api/src/app.test.ts`, `pnpm typecheck` |
| CRM route id validation | CRM client/contact/product/project-type/stage route identifiers accepted unbounded or path-like ids before session, denied audit, and persistence lookup | CRM PATCH route params now use bounded CRM id parsing with stable 400 errors before session or data-source access | `pnpm vitest run apps/api/src/routeParamParsers.test.ts apps/api/src/app.test.ts`, `pnpm typecheck` |
| Absence route and user id validation | Absence delete ids and absence `userId` filters/body values accepted unbounded or path-like strings before resource absence repository calls | Absence delete ids now require UUID shape, and absence user ids use the shared bounded user-id parser for body/query inputs with stable 400 errors | `pnpm vitest run apps/api/src/routeParamParsers.test.ts apps/api/src/app.test.ts`, `pnpm vitest run --config vitest.db.config.ts apps/api/src/absencesRoutes.db.test.ts`, `pnpm typecheck` |
| Absence date and reason validation | Absence create/list accepted regex-only dates and unbounded reason text before persistence, allowing impossible dates, excessive ranges, and oversized metadata | Absence routes now require real ISO dates, cap list/create ranges to 370 days, and bound optional reason text before repository writes or broad reads | `pnpm vitest run --config vitest.db.config.ts apps/api/src/absencesRoutes.db.test.ts` |
| Opportunity route id validation | Project-intake opportunity route ids accepted unbounded or path-like strings before session, service preflight, and opportunity/project activation persistence | Opportunity detail/update/stage/finalize/feasibility/activate routes now use bounded CRM-style opportunity-id parsing with stable 400 errors before session or service access | `pnpm vitest run apps/api/src/routeParamParsers.test.ts apps/api/src/app.test.ts`, `pnpm typecheck`, `pnpm security:check` |
| Closure route id validation | Closure/retrospective project, template-improvement action, and template insight route ids accepted unbounded or path-like strings before session, denied audit, and persistence lookup | Closure read/preview/close/lessons/apply and template retrospective insight routes now parse ids before session/body/persistence with stable 400 errors | `pnpm vitest run apps/api/src/routeParamParsers.test.ts apps/api/src/retrospectiveRoutes.test.ts`, `pnpm typecheck`, `pnpm security:check` |
| CRM activity route id validation | CRM activity entity ids and task activity ids accepted unbounded or path-like strings before session, denied audit, and activity persistence lookup | CRM activity read/comment/task/file/update routes now parse entity type, entity id, and activity id before session/body/persistence with stable 400 errors | `pnpm vitest run apps/api/src/routeParamParsers.test.ts apps/api/src/app.test.ts apps/api/src/crmActivityParsers.test.ts`, `pnpm typecheck`, `pnpm security:check` |
| Node runtime hazard scan | Static scan did not explicitly guard against accidentally committed Node inspector flags or `insecureHTTPParser: true` runtime config | `security:scan` now fails on Node inspector runtime flags anywhere in scanned text files and on insecure HTTP parser usage in API/package runtime source | `pnpm security:scan`, `pnpm security:check` |
| Planning route id validation | Planning read/apply/scenario/solver/saved-view/events routes accepted unbounded route params before session, body parsing, audit, and persistence lookup | Planning project ids, scenario ids, auto-solver run/proposal ids, saved-view ids, and SSE project ids now use bounded shared route parsers with stable 400 errors before backend work | `pnpm vitest run apps/api/src/routeParamParsers.test.ts apps/api/src/app.test.ts`, `pnpm typecheck`, `pnpm security:check` |
| Capacity query id validation | Capacity tree/drilldown accepted raw `projectId` and `resourceId` query strings into cache keys, access checks, and aggregation lookup | Capacity query ids now pass through bounded project/user id parsers before session/cache/aggregation, returning stable `capacity_invalid_query` for malformed ids | `pnpm vitest run --config vitest.db.config.ts apps/api/src/capacityRoutes.db.test.ts`, `pnpm typecheck`, `pnpm security:check` |
| Audit query id validation | Audit event reads accepted raw `projectId` query strings before session and in-memory filtering of audit rows | Audit project filters now use bounded project id parsing before session/persistence, returning stable `invalid_project_id` for malformed ids | `pnpm vitest run apps/api/src/app.test.ts`, `pnpm typecheck`, `pnpm security:check` |
| Audit event read bounds | Audit event reads accepted no limit and Postgres listed the whole tenant audit log before response filtering | Audit route now validates `limit` in the range 1..100 before session work, applies project filtering in the data source, and Postgres queries with a bounded limit | `pnpm vitest run apps/api/src/app.test.ts`, `pnpm vitest run --config vitest.db.config.ts apps/api/src/app.db.test.ts` |
| Scheduled task query validation | Scheduled-task read accepted raw assignee ids and regex-only dates before repository filtering | Scheduled-task query parsing now validates bounded user ids, real ISO dates, date order, and a 370-day range before session or persistence work | `pnpm vitest run apps/api/src/app.test.ts`, `pnpm typecheck`, `pnpm security:check` |
| Production calendar input validation | Calendar reads accepted partially numeric years and bulk exceptions accepted regex-only dates, unbounded arrays/reasons, and raw resource ids | Production calendar routes now require exact year/date shapes, real dates, bounded exception count/reason/minutes/ids, and parsed optional resource ids before persistence | `pnpm vitest run --config vitest.db.config.ts apps/api/src/productionCalendarRoutes.db.test.ts`, `pnpm typecheck`, `pnpm security:check` |
| Org structure replace boundary validation | Org-structure replace accepted path-like node ids, oversized arrays, control-bearing names, and fractional sort order before domain/persistence validation | Replace payload parsing now bounds node/placement counts, validates org/user/position ids, rejects control characters, caps names, and requires safe integer sort order | `pnpm vitest run --config vitest.db.config.ts apps/api/src/orgStructureRoutes.db.test.ts` |
| Dev tenant route id validation | Dev-only tenant/session routes accepted raw query/header user ids and tenant route ids before actor and user persistence lookup | Dev login/current-tenant/user-list routes now use bounded user/tenant id parsing or fail closed before data-source access | `pnpm vitest run apps/api/src/routeParamParsers.test.ts apps/api/src/app.test.ts` |
| Production dev-route fail-closed config | Self-hosted production runtime could be misconfigured to expose dev tenant/session routes through `KISS_PM_ENABLE_DEV_ROUTES=true` | Production startup now rejects enabled dev tenant routes before serving traffic | `pnpm vitest run apps/api/src/serverReadiness.test.ts`, `pnpm typecheck`, `pnpm security:check` |
| Server runtime env parsing | API startup accepted partial numeric `PORT` values and raw `HOST` values directly into the HTTP server | Runtime config now validates exact port range and host shape before server startup, with a single parsed config object used by `server.ts` | `pnpm vitest run apps/api/src/serverReadiness.test.ts`, `pnpm typecheck`, `pnpm security:check` |
| Login credential bounds | Login accepted unbounded credential strings inside a valid JSON body before credential lookup and password verification | Auth login now rejects missing, malformed, or oversized email/password values before rate-limit lookup, repository lookup, or password KDF work | `pnpm vitest run apps/api/src/app.test.ts`, `pnpm typecheck`, `pnpm security:check` |
| Unified search query bounds | Workspace search accepted unbounded or silently coerced `q`/`types`/`limit` query strings and performed session work before rejecting malformed filters | Search now validates bounded query length, strict integer limit, and an allowlisted type filter before session, profile, or source lookup work | `pnpm vitest run apps/api/src/searchRoutes.test.ts apps/api/src/routeHealth.test.ts`, `pnpm typecheck`, `pnpm security:check` |
| Control surface query boolean validation | Control surface list silently treated malformed `includeArchived` query values as `false` after session/profile work | List route now accepts only absent/empty/`true`/`false` values and rejects malformed booleans before session or persistence lookup | `pnpm vitest run apps/api/src/controlSurfaceRoutes.test.ts`, `pnpm typecheck`, `pnpm security:check` |
| JSON Content-Length validation | Shared JSON body reader accepted malformed `Content-Length` headers and only relied on streaming limits afterward | Body reader now rejects malformed/negative/fractional `Content-Length` values with stable 400 before body read while preserving 413 for declared oversized bodies | `pnpm vitest run apps/api/src/jsonBody.test.ts`, `pnpm typecheck`, `pnpm security:check` |
| Attachment upload media-type boundary | File upload route could enter bounded body materialization for non-multipart requests and rely on later form parsing errors | Upload route now requires `multipart/form-data` with a non-empty safe boundary before reading the request body | `pnpm vitest run apps/api/src/attachmentRoutes.test.ts`, `pnpm typecheck` |
| Attachment external metadata sanitization | External reference titles and metadata accepted hidden control characters before persistence, audit-adjacent display, and metadata search reuse | Attachment validators now reject control-bearing external reference titles, metadata keys, metadata strings, and nested array values before persistence | `pnpm vitest run apps/api/src/attachmentValidation.test.ts`, `pnpm typecheck` |
| CRM metadata control-character sanitization | CRM client/contact/product/project-type/stage payloads bounded string lengths but still allowed hidden control characters in display/contact metadata before persistence and audit reuse | CRM parsers now reject control-bearing single-line fields, reject unsafe multiline description controls, and normalize contact email casing before mutation | `pnpm vitest run apps/api/src/crmParsers.test.ts apps/api/src/app.test.ts` |
| Project intake metadata sanitization | Opportunity intake payloads bounded string lengths but still accepted hidden control characters in titles/client names/descriptions/final reasons and prototype-like custom-field keys before persistence and audit reuse | Intake parsers now reject control-bearing single-line metadata, unsafe multiline reason/description controls, unsafe custom-field keys, and control-bearing custom-field values before mutation | `pnpm vitest run apps/api/src/projectIntakeParsers.test.ts apps/api/src/app.test.ts` |
| Planning command metadata bounds | Planning commands accepted unbounded/control-bearing command strings and arbitrary task custom-field JSON before reducer, preview/apply persistence, and audit reuse | Planning parsers now bound command strings, reject control-bearing metadata/scenario reasons/task ids, and allow only safe scalar task custom-field values with safe field keys | `pnpm vitest run apps/api/src/planningParsers.test.ts apps/api/src/planningAutoSolverRoutes.test.ts apps/api/src/app.test.ts` |
| Project work metadata sanitization | Task CRUD/status/comment payloads accepted control-bearing titles/status names/comments, unbounded descriptions, and participant ids validated only by length before task persistence and activity/audit reuse | Project work parsers now reject unsafe single-line task/status metadata, bound and sanitize multiline descriptions/comments, and require safe participant user identifiers before mutation | `pnpm vitest run apps/api/src/projectWorkParsers.test.ts apps/api/src/app.test.ts` |
| CRM activity metadata and legacy file URL sanitization | CRM activity comment/task/file payloads accepted hidden control characters, assignee ids validated only by length, and legacy file URLs weaker than the attachment URL boundary | CRM activity parsers now reject unsafe text/mime metadata, require safe assignee ids, and reuse the external-reference URL validator to block credentials/private hosts/unsafe schemes before activity persistence | `pnpm vitest run apps/api/src/crmActivityParsers.test.ts apps/api/src/app.test.ts apps/api/src/attachmentValidation.test.ts` |
| Workspace config metadata sanitization | Custom-field/template tenant labels and template descriptions accepted hidden control characters before config persistence and audit `commandInput` reuse | Workspace config parsers now reject control-bearing labels and unsafe multiline descriptions before configuration mutation | `pnpm vitest run apps/api/src/workspaceConfigParsers.test.ts apps/api/src/app.test.ts packages/domain/src/workspaceConfig.test.ts` |
| Workspace config denied audit | Workspace config read/create/update denials returned 403 without a management audit trail | Workspace config custom-field/template read and mutation denials now write safe `denied` audit events before returning 403, without loading or mutating config rows | `pnpm vitest run apps/api/src/app.test.ts apps/api/src/workspaceConfigParsers.test.ts` |
| RBAC/user/position denied audit | Access-role, workspace-user, and position permission failures could return 403 without a security-relevant management audit trail | RBAC, user administration, and position read/mutation denials now write safe `denied` audit events before persistence list/mutation work | `pnpm vitest run apps/api/src/app.test.ts` |
| Admin mutation audit atomicity | User deletion and position create/update/delete could mutate before audit succeeded or without requiring transactional audit support | User deletion and position mutations now fail closed when audit/transaction support is unavailable and write the mutation plus audit inside one data-source transaction | `pnpm vitest run apps/api/src/app.test.ts`, `pnpm typecheck` |
| Profile mutation audit atomicity and metadata sanitization | Profile updates and theme changes could mutate before audit succeeded, and profile contact fields accepted control-bearing text before persistence/audit reuse | Profile update/theme mutation now require transaction plus audit support, write mutation and audit atomically, and reject unsafe control-bearing or oversized profile metadata before mutation | `pnpm vitest run apps/api/src/app.test.ts`, `pnpm vitest run --config vitest.db.config.ts apps/api/src/app.db.test.ts`, `pnpm typecheck`, `pnpm security:check` |
| Production calendar audit atomicity and reason sanitization | Tenant calendar bulk updates could persist before audit succeeded, and exception reasons accepted hidden control characters before persistence/audit-adjacent reuse | Bulk production calendar changes now require audit/transaction support, write calendar changes and audit in one transaction, and reject control-bearing reasons before persistence | `pnpm vitest run --config vitest.db.config.ts apps/api/src/productionCalendarRoutes.db.test.ts`, `pnpm typecheck` |
| Absence audit atomicity and reason sanitization | Resource absence create/delete could persist before audit succeeded, and absence reasons accepted hidden control characters before persistence/audit reuse | Absence create/delete now require audit/transaction support, write mutation and audit in one transaction, roll back on audit failure, and reject control-bearing reasons before persistence | `pnpm vitest run --config vitest.db.config.ts apps/api/src/absencesRoutes.db.test.ts`, `pnpm typecheck` |
| Control mutation audit atomicity | KPI definition upsert, KPI evaluation/signal materialization, signal status changes, and corrective-action create/update could persist before the corresponding control audit event succeeded | Control success mutations now require transaction plus audit support and write mutation/audit/action-execution state through the transactional data source, while missing transaction support fails closed before writes | `pnpm vitest run apps/api/src/app.test.ts`, `pnpm vitest run --config vitest.db.config.ts apps/api/src/backendManagementLoop.db.test.ts`, `pnpm typecheck` |
| Task status audit atomicity | Tenant task-status create/update/archive could persist before the corresponding project-work audit event succeeded | Task-status mutations now require transaction plus audit support and write task-status mutation and audit through the transactional data source | `pnpm vitest run --config vitest.db.config.ts apps/api/src/projectWorkRoutes.db.test.ts`, `pnpm typecheck` |
| Control surface draft audit atomicity | Draft surface saves could persist before the corresponding success audit event succeeded | Draft saves now require transaction plus audit support and write draft mutation/audit through the transactional data source, while missing transaction support fails closed before writes | `pnpm vitest run apps/api/src/controlSurfaceRoutes.test.ts`, `pnpm typecheck` |
| Planning proposal audit atomicity | Auto-solver run creation and scenario preview persistence could store proposals before the corresponding audit event succeeded | Solver run and scenario proposal creation now require transaction support and persist proposals plus audit in one data-source transaction | `pnpm vitest run apps/api/src/planningAutoSolverRoutes.test.ts`, `pnpm vitest run apps/api/src/app.test.ts -t planning`, `pnpm typecheck` |
| Org structure audit atomicity | Org-structure replace could commit a tenant hierarchy change before the audit event succeeded | Org-structure replace now runs through the data-source transaction and writes the replacement plus audit through the transactional data source | `pnpm vitest run --config vitest.db.config.ts apps/api/src/orgStructureRoutes.db.test.ts`, `pnpm typecheck` |
| Task comment audit atomicity | Task comments could create an activity row before the corresponding project-work audit event succeeded | Task comment creation now requires transaction plus audit support and writes the activity/audit through the transactional data source | `pnpm vitest run --config vitest.db.config.ts apps/api/src/projectWorkRoutes.db.test.ts`, `pnpm typecheck` |

## Broad verification

- `pnpm test`: passed, 60 files, 432 tests, 1 skipped.
- `pnpm test:db`: passed.
- `pnpm typecheck`: passed for every code slice.
- `git diff --check`: passed for every code slice.
- `pnpm security:check`: passed for backend-scoped audit/scan.

## Remaining Phase 10 audit queue

Open hardening work remains for a later backend security slice: session rotation for future password-reset workflows, deployment-level TLS/reverse-proxy controls that cannot be proven from app code alone, and continued route-by-route validation review for new backend phases. Frontend dependency/CSP work is intentionally outside this backend-only branch. Future backend phases should add their own rows and expand the release-like DB smoke when they introduce a mandatory loop step.
